---
phase: 09-ml-wiring-fix-correct-full-sentence-translation
plan: "02"
type: execute
wave: 2
depends_on: ["09-01"]
files_modified:
  - android/ml/src/main/java/com/vachak/ml/adapter/IndicTrans2Adapter.kt
  - android/ml/src/main/java/com/vachak/ml/SherpaAssets.kt
autonomous: true
requirements: ["MT-01", "MT-03"]
user_setup: []

must_haves:
  truths:
    - "Encoder hidden states [1,seq,512] fed real, not zeros"
    - "Past KV fed with real shapes from ORT, not fixed [1,8,1,64] zeros"
    - "SherpaAssets mt marker is encoder_model.onnx && tokenizer_src.json"
  artifacts:
    - "android/ml/src/main/java/com/vachak/ml/adapter/IndicTrans2Adapter.kt runEncoder+greedyDecode with pastValueShapes"
    - "android/ml/src/main/java/com/vachak/ml/SherpaAssets.kt mt marker"
  key_links:
    - "IndicTrans2Adapter ↔ ORT encoder/decoder/past ↔ SherpaAssets mt dir"
    - "greedyDecode ↔ past KV shape preservation"

---

<objective>
Fix ONNX pipeline voided parts: runEncoder dummy zeros and past KV zeros, plus SherpaAssets mt marker, so full-sentence decode is input-dependent and not constant.

Purpose: Make greedy decode parity with translate.py:141 + it2_onnx_wrappers.py:62, fixing same-output bug. Latency still deferred.
Output: Real encoder hidden states, shape-preserving past, correct argmax/batchDecode, marker fix.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-ml-wiring-fix-correct-full-sentence-translation/09-RESEARCH.md
@.planning/phases/09-ml-wiring-fix-correct-full-sentence-translation/09-PATTERNS.md
@android/ml/src/main/java/com/vachak/ml/adapter/IndicTrans2Adapter.kt
@android/ml/src/main/java/com/vachak/ml/SherpaAssets.kt
@indictrans2-onnx-export/src/translate.py
@indictrans2-onnx-export/src/it2_onnx_wrappers.py
@android/app/src/main/assets/vachak_models/mt/config.json
</context>

<threat_model>
STRIDE: Tampering (past KV injection), DoS (large seq)
ASVS L1: Clamp ids, limit max_new_tokens 128, close tensors
Mitigations: past tensors recreated with flat+shape, try/finally close, ReentrantLock
</threat_model>

<tasks>

<task type="auto">
  <name>Fix runEncoder to feed real hidden states and SherpaAssets mt marker</name>
  <files>android/ml/src/main/java/com/vachak/ml/adapter/IndicTrans2Adapter.kt, android/ml/src/main/java/com/vachak/ml/SherpaAssets.kt</files>
  <read_first>android/ml/src/main/java/com/vachak/ml/adapter/IndicTrans2Adapter.kt, android/ml/src/main/java/com/vachak/ml/SherpaAssets.kt, android/app/src/main/assets/vachak_models/mt/config.json</read_first>
  <action>In IndicTrans2Adapter:
- runEncoder(padded): create inputIdsTensor LongBuffer[1,seq] + maskTensor same, enc.run, get result[0] as OnnxTensor, value float[1][seq][512], flattenEncoderOutput to FloatArray[seq*512] via batch[0] loops hidden 512, close inputs, result.close(), return flat. Create encHiddenTensor FloatBuffer.wrap(flat) [1,seq,512] in greedyDecode.
- flattenEncoderOutput: handle Array<Array<FloatArray>> correctly, pad remainder 0f.
In SherpaAssets: change markerExists else branch for "mt" to File(outDir,"encoder_model.onnx").exists() && File(outDir,"tokenizer_src.json").exists() (was tokens.txt).
</action>
  <verify>grep -n "flattenEncoderOutput" android/ml/src/main/java/com/vachak/ml/adapter/IndicTrans2Adapter.kt && grep -n "mt.*encoder_model" android/ml/src/main/java/com/vachak/ml/SherpaAssets.kt</verify>
  <acceptance_criteria>
    - "runEncoder creates FloatBuffer with [1,seq,512] not dummy zeros"
    - "SherpaAssets mt marker checks encoder_model.onnx"
  </acceptance_criteria>
  <done>Encoder real + marker fixed</done>
</task>

<task type="auto">
  <name>Fix greedyDecode past KV shape preservation + argmax + batchDecode</name>
  <files>android/ml/src/main/java/com/vachak/ml/adapter/IndicTrans2Adapter.kt</files>
  <read_first>android/ml/src/main/java/com/vachak/ml/adapter/IndicTrans2Adapter.kt, indictrans2-onnx-export/src/translate.py, indictrans2-onnx-export/src/it2_onnx_wrappers.py</read_first>
  <action>Rewrite greedyDecode:
- Keep past as List<Pair<Any,LongArray>> pastValueShapes where each is (value=OnnxTensor.value, shape=OnnxTensor.info.shape). On step0: result=dec.run(feeds with encoder_hidden_states), logits=result[0].value, pastValueShapes=(1 until result.size()).map{ t=result[it] as OnnxTensor; Pair(t.value as Any, t.info.shape) }, result.close()
- On step>0: pastMap=buildPastTensorsFromValueShapes(env, pastValueShapes, encSeqLen) where for each layer base 4, toTensor(pair) does flattenPastValue(obj, shape.fold) then OnnxTensor.createTensor(env, FloatBuffer.wrap(flat), shape) using actual shape from pair (not fixed [1,8,1,64]). Feeds putAll pastMap, result=decPast.run(feeds), update pastValueShapes similarly, close pastMap tensors after.
- Fix argmaxLogits to handle float[1][1][vocab] as Array<Array<FloatArray>> last = batch[0].last() max, also handle FloatArray fallback.
- Fix batchDecode: tokens=ids.drop(1).takeWhile{eos/pad}.mapNotNull{tgtIdToToken[it]} then join("") (not " ") then replace "▁"->" " then Regex(" {2,}")->" " trim.
- Keep maxTokens 128, clamp nextId < tgtDictSize else unkId, Log Vachak-MT decode step.
</action>
  <verify>grep -n "pastValueShapes" android/ml/src/main/java/com/vachak/ml/adapter/IndicTrans2Adapter.kt && grep -n "buildPastTensorsFromValueShapes" android/ml/src/main/java/com/vachak/ml/adapter/IndicTrans2Adapter.kt && grep -n "joinToString(\"\")" android/ml/src/main/java/com/vachak/ml/adapter/IndicTrans2Adapter.kt</verify>
  <acceptance_criteria>
    - "greedyDecode uses pastValueShapes with real shapes"
    - "batchDecode joinToString(\"\") present"
    - "argmax handles float[][][]"
  </acceptance_criteria>
  <done>Past KV and decode fixed</done>
</task>

<task type="auto">
  <name>Ensure ORT sessions pack-aware reload and sequential lock</name>
  <files>android/ml/src/main/java/com/vachak/ml/adapter/IndicTrans2Adapter.kt</files>
  <read_first>android/ml/src/main/java/com/vachak/ml/adapter/IndicTrans2Adapter.kt</read_first>
  <action>Verify ensureLoaded: resolveBaseDir via PackManager.getActivePackFor + SherpaAssets.prepare, synchronized closeSessions on baseDir change, env=OrtEnvironment.getEnvironment(), opts inter/intra 1, createSession encoder/decoder/decoder_with_past, loadVocabs (mergesRank), numLayers=(decoder.numOutputs-1)/4. Keep ReentrantLock in translate, Log tag Vachak-MT at input/tokenize/decode/output/latency. Ensure no INTERNET permission check passes.</action>
  <verify>grep -n "ReentrantLock" android/ml/src/main/java/com/vachak/ml/adapter/IndicTrans2Adapter.kt && grep -n "Vachak-MT" android/ml/src/main/java/com/vachak/ml/adapter/IndicTrans2Adapter.kt | head -5</verify>
  <acceptance_criteria>
    - "ReentrantLock present"
    - "Vachak-MT logs present"
  </acceptance_criteria>
  <done>Session lifecycle correct</done>
</task>

</tasks>

<artifacts>
Artifacts this phase produces:
- android/ml/src/main/java/com/vachak/ml/adapter/IndicTrans2Adapter.kt (ONNX pipeline)
- android/ml/src/main/java/com/vachak/ml/SherpaAssets.kt (mt marker)
</artifacts>
