---
phase: 09-ml-wiring-fix-correct-full-sentence-translation
plan: "03"
type: execute
wave: 3
depends_on: ["09-02"]
files_modified:
  - android/app/src/main/java/com/vachak/ui/screens/LiveScreen.kt
  - android/app/build/outputs/apk/debug/app-debug.apk
  - THIRD_PARTY_NOTICES.md
autonomous: false
requirements: ["MT-01", "MT-02", "MT-03"]
user_setup: []

must_haves:
  truths:
    - "Typing 'एक' and 'पानी' in LiveScreen shows distinct Santali (not same garbage)"
    - "Mic Hindi 'बच्चों, पाँच आम गिनो।' shows live Hindi transcription + varied Santali + audio"
    - "APK rebuilt with fixed :ml adapter, manual verify Live typing+mic produce varied Santali"
  artifacts:
    - "android/app/build/outputs/apk/debug/app-debug.apk rebuilt"
    - "LiveScreen.kt debounce 600ms preview wired to real adapter"
  key_links:
    - "LiveScreen manualHindi → IndicTrans2Adapter.translate → Santali preview → ConversationMessagePair"
    - "APK → on-device mt bundle → offline fonts Noto Ol Chiki"

---

<objective>
Integrate and verify end-to-end: Live typing + mic produce varied correct Santali Ol Chiki full sentences, rebuild APK, manual demo.

Purpose: Prove pipeline traced App input → preprocess→BPE→encoder→decoder+past → postprocess → Live UI, not same-output. Latency ignored per phase brief.
Output: Rebuilt APK, log evidence, THIRD_PARTY_NOTICES font provenance.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-ml-wiring-fix-correct-full-sentence-translation/09-RESEARCH.md
@.planning/phases/09-ml-wiring-fix-correct-full-sentence-translation/09-PATTERNS.md
@android/app/src/main/java/com/vachak/ui/screens/LiveScreen.kt
@android/ml/src/main/java/com/vachak/ml/adapter/IndicTrans2Adapter.kt
@THIRD_PARTY_NOTICES.md
</context>

<threat_model>
STRIDE: Information disclosure (logcat contains Hindi), Denial (mic permission)
ASVS L1: No PII logged beyond truncated 80 chars, mic permission via RECORD_AUDIO launcher
Mitigations: Log text.take(80), permission check before AudioRecord
</threat_model>

<tasks>

<task type="auto">
  <name>Verify LiveScreen wiring (input → adapter → preview → conversation)</name>
  <files>android/app/src/main/java/com/vachak/ui/screens/LiveScreen.kt</files>
  <read_first>android/app/src/main/java/com/vachak/ui/screens/LiveScreen.kt, android/ml/src/main/java/com/vachak/ml/adapter/IndicTrans2Adapter.kt</read_first>
  <action>Confirm LiveScreen:
- manualHindi state + LaunchedEffect(manualHindi) debounce 600ms calls engine.translation.translate(q, LanguagePair("hi","sat_Olck")) on Dispatchers.IO, updates livePreview/livePreviewLoading, Surface "Santali · Live" shows varied text (18sp, VachakFontFamily), Play button calls tts.synthesize(sat,"sat_Olck") + tryPlayPcm(22050), Add to conversation calls translateAndAppend
- translateAndAppend creates ConversationItem with isTranslating true, launches Dispatchers.IO mtStart, engine.translation.translate, updates LiveConversationStore, fires TTS synthesize in background, logs Vachak-MT
- mic path startRecordingUktamStyle → StreamingAsrSession + AudioCapturer → stopRecordingAndProcessUktamStyle → engine.asr.transcribe → translateAndAppend
- Ensure no auth gate: MainActivity uses EngineProvider.real
- If missing, patch LiveScreen to add preview surface above LiveInputBar as per 08 wiring.
</action>
  <verify>grep -n "livePreview" android/app/src/main/java/com/vachak/ui/screens/LiveScreen.kt | head -20 && grep -n "translateAndAppend" android/app/src/main/java/com/vachak/ui/screens/LiveScreen.kt</verify>
  <acceptance_criteria>
    - "LiveScreen.kt contains livePreview debounce"
    - "LiveScreen.kt calls engine.translation.translate with sat_Olck"
  </acceptance_criteria>
  <done>Live wiring verified</done>
</task>

<task type="auto">
  <name>Rebuild APK with fixed adapter and verify bundle+fonts</name>
  <files>android/app/build/outputs/apk/debug/app-debug.apk</files>
  <read_first>android/ml/src/main/java/com/vachak/ml/adapter/IndicTrans2Adapter.kt, android/ml/src/main/java/com/vachak/ml/SherpaAssets.kt</read_first>
  <action>Run JAVA_HOME=/tmp/jdk17 ./gradlew -p android assembleDebug (needs JDK 17). Verify unzip -l app-debug.apk contains res/font/noto_sans_ol_chiki.ttf, res/font/noto_sans_devanagari.ttf, vachak_models/mt/encoder_model.onnx (or via merged assets). Check THIRD_PARTY_NOTICES.md contains Noto OFL + IndicTrans2 MIT. Ensure no INTERNET permission in manifest.</action>
  <verify>ls -lh android/app/build/outputs/apk/debug/app-debug.apk && unzip -l android/app/build/outputs/apk/debug/app-debug.apk | grep -E "noto|encoder_model" | head -10</verify>
  <acceptance_criteria>
    - "app-debug.apk exists and contains encoder_model.onnx"
    - "apk contains noto_sans_ol_chiki.ttf"
  </acceptance_criteria>
  <done>APK rebuilt</done>
</task>

<task type="auto">
  <name>Manual verification: varied full-sentence translations</name>
  <files>android/app/src/main/java/com/vachak/ui/screens/LiveScreen.kt</files>
  <read_first>android/app/src/main/java/com/vachak/ui/screens/LiveScreen.kt</read_first>
  <action>Manual steps (autonomous false — requires device):
1. Install APK: adb install android/app/build/outputs/apk/debug/app-debug.apk
2. Open Live tab (Hindi → Santali badge)
3. Type "एक" → preview shows "ᱢᱤᱫᱴᱟᱝ" (distinct)
4. Clear, type "पानी" → shows "ᱫᱟᱜ" (different from previous)
5. Type "बच्चों, पाँच आम गिनो।" → shows "ᱜᱤᱫᱽᱨᱟᱹ..." not same as "नमस्ते" which should show "ᱡᱚᱦᱟᱨ" (curated)
6. Mic: hold FAB, say "मैं ठीक हूँ" → live transcription strip shows Hindi, then Santali "ᱤᱧ ᱱᱟᱯᱟᱭ..." + Play audio 22050Hz
7. Capture logcat: adb logcat -s Vachak-MT:V Vachak-ASR:V Vachak-Latency:V | grep -E "input:|tokenize:|decode step|OlChikiValidator"
8. Verify varied ids: tokenize ids for एक [8,29925,34,2] vs पानी [8,29925,1550,2] not all 3s, and decode steps varied nextId.
Document in .planning/phases/09-*/verification-notes.md with screenshots + log snippet.
</action>
  <verify>cat .planning/phases/09-ml-wiring-fix-correct-full-sentence-translation/verification-notes.md 2>&1 | head -20 || echo "manual verification pending — create verification-notes.md with logcat snippet"</verify>
  <acceptance_criteria>
    - "verification-notes.md contains varied translations for एक vs पानी"
    - "logcat shows tokenize ids varied and OlChikiValidator PASS"
  </acceptance_criteria>
  <done>Manual verification documented</done>
</task>

</tasks>

<artifacts>
Artifacts this phase produces:
- android/app/build/outputs/apk/debug/app-debug.apk (rebuilt)
- .planning/phases/09-ml-wiring-fix-correct-full-sentence-translation/verification-notes.md
</artifacts>
