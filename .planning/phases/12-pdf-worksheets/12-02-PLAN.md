---
phase: 12-pdf-worksheets
plan: "02"
type: execute
wave: 2
depends_on: ["12-01"]
files_modified:
  - "android/app/src/main/java/com/vachak/ui/content/PackContentReader.kt"
  - "android/app/src/main/java/com/vachak/ui/pdf/PdfViewer.kt"
  - "android/app/src/main/java/com/vachak/ui/screens/PackStudyViews.kt"
  - "android/app/src/main/java/com/vachak/ui/screens/ChapterStudyScreens.kt"
  - "android/app/src/test/java/com/vachak/PackContentReaderPdfTest.kt"
  - "android/app/src/test/java/com/vachak/PdfViewerStateTest.kt"
  - "android/app/src/test/java/com/vachak/PackWorksheetImageTest.kt"
autonomous: true
requirements: ["WS-02", "WS-03", "WS-05", "WS-06"]
user_setup:
  - "Emulator or tablet with the v0.3.0 content pack side-loaded for the manual 30-slot open check (VALIDATION.md table)"

must_haves:
  truths:
    - "Explicit G1/G3 chapters render converted JSON: worksheets with hi+Ol Chiki prompts AND page thumbnails, decks with Ol Chiki backs AND art (existing deck path, refs populated by converter)"
    - "Pool-backed slots render their PDF in-app (PdfRenderer pager); missing/unreadable PDFs show honest Failed state with external-viewer fallback, never a crash"
    - "PackContentReader.openWorksheetPdf() resolves installed pack -> bundled .vachakpack -> cache materialize (capped), null-on-missing, zero throws to UI"
    - "Non-explicit grades' placeholder decks are byte-identical to pre-phase; only the 9 user-supplied G1/G3 decks change (DRAFT preserved)"
    - "One live bitmap at a time per image surface (RGB_565, recycled on change/dispose); no parallel decode; no INTERNET permission added"
    - "JVM unit tests for reader path + viewer state + worksheet images pass; full :app:testDebugUnitTest green"
  artifacts:
    - "android/.../ui/pdf/PdfViewer.kt (in-app PdfRenderer pager composable)"
    - "PackContentReader.openWorksheetPdf() + PdfRef + PackQuestion.imageRef (additive extensions)"
    - "PackWorksheetView thumbnails + ChapterWorksheetScreen dual host (JSON-first, PDF branch)"
  key_links:
    - "converted ws JSON -> PackContentReader.parseWorksheetJson -> PackWorksheetView (explicit slots); chapter.json worksheet_pdf -> openWorksheetPdf() -> cache File -> PdfViewer (pool slots) — the two ground truths for 'opens in app'"
---

<objective>
Android runtime for BOTH slot kinds: (1) explicit G1/G3 chapters render
converted JSON through existing views plus additive worksheet thumbnails
(deck art already works — converter populates image_ref); (2) pool-backed
slots resolve their worksheet_pdf ref to a local file and render in-app with
a bounded PdfRenderer pager. Non-explicit decks untouched.

Purpose: Make all 30 slots actually OPEN on-device, offline, within 2 GB RAM
discipline — authored chapters with text+art, pool chapters as PDFs — with
honest empty/failed states instead of crashes. Output: reader extensions +
thumbnails + viewer + tests, verified on JVM and (tagged manual) on device.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/12-pdf-worksheets/12-CONTEXT.md
@.planning/phases/12-pdf-worksheets/12-RESEARCH.md
@.planning/phases/12-pdf-worksheets/12-VALIDATION.md
@.planning/phases/12-pdf-worksheets/12-01-PLAN.md
@.planning/phases/12-pdf-worksheets/authored_mapping.json
@.planning/phases/12-pdf-worksheets/pdf_slot_mapping.json
@android/app/src/main/java/com/vachak/ui/content/PackContentReader.kt
@android/app/src/main/java/com/vachak/ui/pdf/WorksheetPdf.kt
@android/app/src/main/java/com/vachak/ui/screens/ChapterStudyScreens.kt
@android/app/src/main/java/com/vachak/ui/screens/PackStudyViews.kt
@android/app/src/main/assets/curriculum/pack_summary.json
</context>

<threat_model>
STRIDE: Spoofing (pool PDF bytes sha256-checked before render; converted JSON parsed with the existing guarded parsers — corrupt files recycle into Failed/empty states), Tampering (cache materialize writes ONLY under cacheDir/bundled_pdf/, basename of allow-listed refs — no `..` traversal; worksheet/deck image_refs resolve ONLY under the chapter dir or the APK flashcard tree, same `decodeDeckImage` boundary; FileProvider fallback uses existing authority), Denial (30 MB materialize cap + 200-page cap + one live bitmap per surface + cooperative cancellation via existing ctxJob pattern; corrupt PDFs/webp recycle into Failed states, never retry loops), Information Disclosure (none — error strings carry file names only, never content).
ASVS L1: No network (manifest INTERNET-grep asserted in T4); no new permissions; no WebView/JS (PdfRenderer only); existing SAF/pack-install trust boundary unchanged.
Mitigations: reuse `readEntryCapped` for ALL zip reads; reuse RGB_565 + recycle discipline for thumbnails; every new public fun null-safe/`runCatching`-guarded like the rest of PackContentReader.
</threat_model>

<tasks>

- [ ] **T1 — openWorksheetPdf() pool-reader path + JVM tests (WS-02)**
  <read_first>
  - `android/app/src/main/java/com/vachak/ui/content/PackContentReader.kt` (full: `worksheetFile`, `readWorksheetBlocking`, `materializeBundledChapter`, `readEntryCapped`, `loadChapterBundle`, `bundleFromDir` — extend these, don't fork)
  - `.planning/phases/12-pdf-worksheets/pdf_slot_mapping.json` (ref shape: `pdf_pool/<basename>`)
  </read_first>
  <action>
  In `PackContentReader.kt` add (no existing fun signature changes):
  1. `data class PdfRef(val relativePath: String, val sha256: String?)` + pure `parsePdfRef(chapterJsonText: String): PdfRef?` (reads `worksheet_pdf` + `pdf_sha256`; null when absent — explicit-JSON chapters use the question path, NOT this one).
  2. `fun resolvePdfRef(ref: PdfRef): File?` pure-ish resolver used by tests: rejects absolute paths and any `..` segment (returns null), else maps to `pdf_pool/<basename>`.
  3. `suspend fun openWorksheetPdf(context, grade, slug): File?` (plus `openWorksheetPdfBlocking` mirroring `readWorksheetBlocking`): installed-pack chapter dir → read chapter.json → resolve ref → file exists + sha256 matches (mismatch => null + notice) → return. Else bundled `.vachakpack` single-scan for `curriculum/class/{g}/chapters/{slug}/chapter.json` (get ref) then `curriculum/class/pdf_pool/<basename>` entry → materialize via `readEntryCapped(30 MB)` to `cacheDir/bundled_pdf/` → sha256-check → return. Every step `runCatching`; null on ANY miss. Extend `ChapterBundle` with `worksheetPdf: File?` (nullable, additive) populated in BOTH `bundleFromDir` and the bundled branch; extend `notices` with PDF-specific honest strings ("Worksheet PDF not in pack", "Worksheet PDF failed checksum — hidden", "Textbook PDF — speaker review pending").
  4. `parseWorksheetJson` EXTEND (additive): also read `prompt_hi` (new key from converter) and `image_ref` into new optional `PackQuestion.imageRef: String?` + `promptHi: String?` fields — absent keys => null, all existing tests keep passing UNCHANGED. Same for `parseDeckJson`: read `image_ref` into existing `PackCard.imageRef` (already there — verify, don't duplicate).
  5. Add `PackContentReaderPdfTest.kt` (JVM, no Context): `parsePdfRef` present/absent/malformed; `resolvePdfRef` traversal-rejection table (`../x`, `/abs`, `pdf_pool/ok.pdf`); `parseWorksheetJson` backward-compat (old JSON without new keys parses identically) + new-keys parsing (image_ref/hi land in the new fields); bundle-notice strings non-blank. Run green.
  </action>
  <acceptance_criteria>
  - `PackContentReader.kt` contains `openWorksheetPdfBlocking`, `parsePdfRef`, `resolvePdfRef`, `ChapterBundle.worksheetPdf`, `PackQuestion.imageRef`/`promptHi`; `git diff` shows zero changes to `parseDeckJson` logic, `readDeckBlocking`, deck branches.
  - `PackContentReaderPdfTest` N/N green; ALL pre-existing reader tests still green unmodified.
  - Non-explicit deck files untouched (no test or code path rewrites deck JSON).
  </acceptance_criteria>

- [ ] **T2 — Worksheet thumbnails in PackWorksheetView + tests (WS-03 explicit slots)**
  <read_first>
  - `android/app/src/main/java/com/vachak/ui/screens/PackStudyViews.kt` (full — `PackWorksheetView` structure + `PackDeckView` art idiom to mirror: LaunchedEffect decode, recycle on dispose)
  - `android/app/src/main/java/com/vachak/ui/content/PackContentReader.kt` `decodeDeckImage` (reuse for worksheet image_refs — chapter-dir-relative webp included)
  </read_first>
  <action>
  1. `decodeDeckImage` generalizer (MINIMAL): accept chapter-dir-relative `pages/*.webp` refs by also trying the chapter's `pages/` dir — implement as `decodeQuestionImage(context, grade, slug, imageRef)` reusing the SAME caps (maxDim 512, 4 MB entry cap, RGB_565, null-on-miss). No signature changes to `decodeDeckImage`.
  2. `PackWorksheetView`: when `q.imageRef` non-null, show a 160dp thumbnail ABOVE the prompt (same RoundedCornerShape(16.dp) + border idiom as deck art), decoded in `LaunchedEffect(q.id)` with recycle-on-dispose; null art => text-only (no placeholder box, no crash). Prompt line shows `prompt_sat_deva` primary + `promptHi` secondary (small, TextSecondary) when present — mirrors the chapter Read section's script honesty.
  3. `PackViewerStateTest`-style JVM test `PackWorksheetImageTest.kt` for the pure parts: extract `internal fun questionImageKey(grade, slug, ref): String?` (returns null for blank/absolute/`..` refs) and unit-test the table. Image DECODE itself is manual-covered (emulator, VALIDATION table).
  4. Deck rendering: NO CODE — verify by test that `parseDeckJson` maps converter output (`concept_hi`, `concept_target` Ol Chiki, `image_ref`) into existing fields (add 3 assertions to the T1 test class if not already covered).
  </action>
  <acceptance_criteria>
  - Thumbnail path present with recycle-on-dispose (`recycle()` in a `DisposableEffect`); null-art renders text-only (prove via JVM test on the key helper + code read).
  - `PackDeckView` zero diff (`git diff --stat` excludes it).
  - `:app:testDebugUnitTest --tests '*PackWorksheetImage*'` green.
  </acceptance_criteria>

- [ ] **T3 — PdfViewer.kt pager + worksheet host wiring (WS-03 pool slots)**
  <read_first>
  - `android/app/src/main/java/com/vachak/ui/screens/ChapterStudyScreens.kt` (full: `loadBundle`, `StudySubHeader`, `CenterNote`, `LoadingNote`, `ChapterWorksheetScreen` — host here)
  - `android/app/src/main/java/com/vachak/ui/pdf/WorksheetPdf.kt` (fallback `viewIntent`/`shareIntent` reuse)
  - `android/app/src/main/java/com/vachak/ui/theme/` (VachakColors, tabletHPad, cardShadow names to reuse)
  </read_first>
  <action>
  1. Create `android/app/src/main/java/com/vachak/ui/pdf/PdfViewer.kt`: `@Composable fun PdfViewer(pdfFile: File, modifier)` — `ParcelFileDescriptor` + `PdfRenderer` in `DisposableEffect(pdfFile)` (close both on dispose); `pageIndex` state (0..pageCount-1, Previous/Next + `i / n` monospace counter reusing PackDeckView styling); renders CURRENT page only via `LaunchedEffect(pdfFile, pageIndex)` into RGB_565 at display width (min(pdfWidth, 1080px) — never print dpi); `recycle()` previous bitmap before swap and on dispose; `runCatching` → `Failed(reason)` with `CenterNote`-style text + "Open externally" button (existing `WorksheetPdf.viewIntent`). Caps: refuse > 30 MB or pageCount > 200 (`MAX_PDF_BYTES`, `MAX_PDF_PAGES` top-of-file constants).
  2. Rewire `ChapterWorksheetScreen`: after `loadBundle`, branch — `bundle.worksheetPdf != null` → `StudySubHeader(count = "<n> pages")` + `PdfViewer`; else EXISTING `PackWorksheetView(qs)` (explicit + legacy JSON, now with thumbnails from T2). Keep Loading/Failed states; show the AUTO_EXTRACTED/DRAFT notice line above content when present in `bundle.notices`.
  3. Do NOT modify `ChapterDeckScreen`, `PackDeckView`, `GradeScreen`, `ChapterScreen` beyond compilation (ideally zero diff — prove with `git diff --stat`).
  4. `PdfViewerStateTest.kt` (pure helpers `clampPage`/`rejectFile` as `internal` funs; boundary table incl. negative index, over-count, 30 MB+1, 201 pages).
  </action>
  <acceptance_criteria>
  - `PdfViewer.kt` exists with `PdfRenderer`, single-bitmap discipline (`recycle()` >= 2 sites), caps as literals.
  - `ChapterWorksheetScreen` compiles with both paths; legacy JSON path lines unchanged except the branch.
  - `PdfViewerStateTest` green; `git diff --stat` shows no `*Deck*`, no `GradeScreen.kt`, no `ChapterScreen.kt`, no deck JSON modifications.
  </acceptance_criteria>

- [ ] **T4 — Full verification: suites + permission/size + manual handoff (WS-05, WS-06)**
  <read_first>
  - `.planning/phases/12-pdf-worksheets/12-VALIDATION.md` (manual table = handoff checklist)
  - `android/app/src/main/AndroidManifest.xml` (INTERNET must be absent)
  </read_first>
  <action>
  1. Run from repo root: `/usr/bin/python3 -m unittest discover -s curriculum/tests`, `bash scripts/run_offline_tests.sh`, `cd android && ./gradlew :ml:testDebugUnitTest :app:testDebugUnitTest` — paste results; all green or unrelated-failure evidence (unmodified failing file + `git status` proof).
  2. Static guards: `grep -rn "android.permission.INTERNET" android/app/src/main/AndroidManifest.xml` MUST print nothing; `grep -rn "import.*WebView\|loadUrl\|http://\|https://" android/app/src/main/java/com/vachak/ui/pdf/ android/app/src/main/java/com/vachak/ui/content/` MUST print nothing; `grep -rn "launch\|async(" android/app/src/main/java/com/vachak/ui/pdf/PdfViewer.kt` MUST print nothing.
  3. Sequential-inference untouched: `grep -rn "numThreads" android/ml/.../adapter/ | head` still `= 1`, `git diff --stat android/ml/` empty.
  4. Deck-diff proof: `git diff --stat` lists no deck JSON, no `PackDeckView`, no `ChapterDeckScreen`; the ONLY deck-content change in the universe is the 9 converter-produced `deck_bilingual.json` files inside the (gitignored) pack — prove via zip listing + DRAFT grep.
  5. Write the manual handoff block: 30 slots as checkboxes SPLIT by kind (9–10 explicit incl. filling-and-lifting-reserved + 20 pool) with per-kind checks (explicit: prompts hi+Ol Chiki, thumbnails, deck Ol Chiki backs, webp art; pool: PDF pages forward/back, rotation, background-foreground) + WiFi-OFF procedure + deck-regression rows (≥1 untouched deck per pool grade).
  </action>
  <acceptance_criteria>
  - All suites green (or documented-unrelated with proof); all three greps as specified; ml untouched.
  - Deck-diff proof commands pasted with outputs.
  - Summary contains the split 30-checkbox handoff list ready for device verification.
  </acceptance_criteria>

</tasks>

<verification>
- `:app:testDebugUnitTest` fully green (new tests + zero regressions in deck/worksheet-JSON tests).
- `git status --short android/` shows ONLY: `ui/content/PackContentReader.kt`, `ui/pdf/PdfViewer.kt` (new), `ui/screens/PackStudyViews.kt`, `ui/screens/ChapterStudyScreens.kt`, three test files — zero deck files, zero manifest diff, zero `android/ml/` diff.
- New-code image discipline: at most ONE live `Bitmap` per surface; `RGB_565` present in both image paths; `recycle()` >= 2 sites each; caps as literals.
- No INTERNET string in manifest; no WebView/http imports in new path; no coroutine launch in viewer.
- DRAFT/neutral review signals preserved end-to-end: converter `needs_review: ["DRAFT"]` renders through the existing amber affordances (prove with one screenshot-row in the manual handoff or a JVM assertion on the notice strings).
- UI-SPEC waiver recorded (unchanged): narrow viewer + thumbnail addition reusing VachakColors/tabletHPad/cardShadow + PackDeckView pager idiom; no new design language; manual visual check covers it.
</verification>

<success_criteria>
- [ ] Explicit G1/G3 slots render JSON worksheets (hi+Ol Chiki+thumbnails) and Ol Chiki decks with art
- [ ] Pool slots open PDFs in-app; missing/corrupt PDFs Fail honestly with external fallback
- [ ] Non-explicit decks byte-identical; only 9 supplied decks change (DRAFT preserved)
- [ ] Memory discipline both paths; offline + sequential proofs hold
- [ ] Split device handoff list written; VALIDATION.md manual table executable as-is
</success_criteria>

## Artifacts this phase produces

- `PackContentReader.PdfRef` — `{relativePath, sha256}` (new data class)
- `PackContentReader.parsePdfRef / resolvePdfRef / openWorksheetPdfBlocking / openWorksheetPdf / decodeQuestionImage / questionImageKey` — new functions (additive; existing signatures unchanged)
- `ChapterBundle.worksheetPdf: File?` — new nullable field (additive)
- `PackQuestion.imageRef: String?` + `promptHi: String?` — new optional fields (additive)
- `android/.../ui/pdf/PdfViewer.kt` — `PdfViewer()` + `clampPage`/`rejectFile` + caps (new file)
- `PackWorksheetView` thumbnail branch + `ChapterWorksheetScreen` dual host (additive branches)
- `PackContentReaderPdfTest.kt`, `PdfViewerStateTest.kt`, `PackWorksheetImageTest.kt` — new JVM tests
- Nothing else. In particular: no new permissions, no new dependencies, no nav-route changes, no non-explicit deck changes.
