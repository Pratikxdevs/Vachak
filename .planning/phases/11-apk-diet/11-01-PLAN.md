---
phase: 11-apk-diet
plan: "01"
type: verify
wave: 1
depends_on: []
files_modified: []
autonomous: true
requirements: ["SIZE-01", "SAFE-01", "PERF-01"]
user_setup: []

must_haves:
  truths:
    - "Release tablet APK measures <500MB OR the exact gap is quantified with per-file attribution and no file was deleted or modified to get there"
    - "Every APK asset >1MB carries a verdict: LIVE (with code reference), DEAD (with proof of no consumer), or NEEDS-DECISION (with scope impact)"
    - "The 28MB LoRA adapters are proven consumed-or-dead by code trace, not by filename guess"
    - "Device-side storage (APK + filesDir extraction + pack extract) is measured after cold start, not estimated"
    - "No source file, asset, or repo file is deleted, moved, or edited by any 11-01 task"
  artifacts:
    - ".planning/phases/11-apk-diet/11-VERIFICATION.md (budget table + verdict register + edge cases)"
  key_links:
    - "EngineProvider.real() -> AdapterTranslationEngine.translate() routing -> asset consumed (the only ground truth for LIVE verdicts)"
---

<objective>
Verify-only audit: prove exactly where the ~770MB (fresh-build projection)
goes, classify every megabyte as LIVE/DEAD/NEEDS-DECISION with code evidence,
and quantify the <500MB gap — without deleting, moving, or editing anything.

Purpose: Give the user a decision-grade deletion shortlist where each row has
a byte size, a code reference, and a quality-impact statement, so 11-02
execution can cut ~270MB with zero APK quality risk.
Output: 11-VERIFICATION.md with measured release numbers + verdict register
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/11-apk-diet/11-CONTEXT.md
@android/app/src/main/java/com/vachak/engine/EngineProvider.kt
@android/ml/src/main/java/com/vachak/ml/adapter/AdapterTranslationEngine.kt
@android/ml/src/main/java/com/vachak/ml/adapter/IndicTrans2Adapter.kt
@android/ml/src/main/java/com/vachak/ml/adapter/OnnxIndicTrans2Adapter.kt
@android/app/build.gradle.kts
</context>

<threat_model>
STRIDE: Tampering (verdicts must cite code, never filenames), Denial (cold-start extraction on 8GB tablets), Information Disclosure (none — no logs added)
ASVS L1: No code changes at all in this plan; read-only commands only (ls/du/unzip/grep/onnx-inspect). If any task needs a build, it must use a COPY of the repo or a temp flavor, never the working tree.
Mitigations: verdicts require grep+path evidence; size claims require measured APK (release tablet flavor) or explicit PROJECTION label
</threat_model>

<tasks>

- [ ] **T1 — Fresh release measurement (ground truth)**
  Run: `./gradlew :app:assembleTabletRelease` (or document why not + use
  `bundletool`-free `unzip -l` on the debug APK with per-file table as
  fallback, labeled PROJECTION). Record: APK bytes, top-30 entries via
  `unzip -l`, `classes*.dex` total, `lib/arm64-v8a` total, `assets/*` total,
  confirmation that NO `x86_64` ships in tablet flavor.
  Truth: release number replaces the 690MB-debug / 770MB-projection with a
  measured value; delta vs projection explained line by line.

- [ ] **T2 — MT duality verdict (the ~200-360MB question)**
  Trace from `EngineProvider.real()` → `AdapterTranslationEngine.translate()`:
  confirm Santali path consumes ONLY `vachak_models/mt/*` (ONNX) and Mundari
  path consumes ONLY `modelpacks/stripped_mt_merged/*` (CT2) + phrasebook
  fallback. Record per-path byte totals. Verdict rows for: ONNX bundle,
  CT2 model.bin, CT2 vocabs, phrasebook, EACH adapter safetensors.
  Truth: each MT asset has a LIVE verdict with the exact consuming call site,
  or DEAD with the negative grep evidence (safetensors: extraction+log only,
  no inference load — re-verify, do not trust CONTEXT).

- [ ] **T3 — Adapter safetensors proof (28MB, zero-quality-risk candidate)**
  Exhaustive search for any consumer of `adapter_model.safetensors` bytes:
  grep `safetensors|peft|LoRA|lora` across `android/*/src/main/java`;
  confirm ONNX graph cannot take LoRA (structural argument) and CT2
  `model.bin` is pre-merged (`merge_and_unload` provenance in repo docs).
  Check `ensureAdapterExtracted` callers: is extraction load-bearing for
  `isReady()`/diagnostics even if weights are unused?
  Truth: verdict DEAD (safe to exclude from APK packaging) or LIVE with the
  consumer call chain — plus a note if `isReady()` needs a stub when excluded.

- [ ] **T4 — Tokenizer prune sizing (48MB JSONs)**
  Measure: token-id coverage of `tokenizer_src/tgt.json` against (a) the
  clamp logic in `OnnxIndicTrans2Adapter`/`SpApproxTokenizer`, (b) a
  10k-sentence Hindi+Ol Chiki corpus pass counting used ids. Report pruned
  size estimate (vocab subset + merges subset) WITHOUT writing pruned files.
  Truth: projected saving in MB with method documented, quality risk stated
  (OOV fallback behavior on pruned ids).

- [ ] **T5 — TTS/ASR quant headroom (110MB + 134MB)**
  Report-only: TTS 110MB FP32 Coqui VITS vs 80MB budget (gap 30MB) —
  estimate dynamic-int8 size via a COPY in /tmp (never the asset), record
  estimate only. ASR 134MB NeMo CTC: identify the exact model card/config
  (vocab 5633, Bengali-script tokens — flag Hindi-WER verification as open
  question), list smaller shap-compatible alternatives WITHOUT downloading.
  Truth: numbers + options, no asset touched.

- [ ] **T6 — Device-side storage + cold-start measurement**
  On the emulator (or document device procedure): fresh install → record
  APK bytes, `filesDir` bytes after first launch of EACH engine
  (which assets `SherpaAssets.prepare` + pack extraction actually copy),
  peak transient during copy. List what extracts eagerly vs lazily with code
  refs. Truth: total device footprint number + eager/lazy table.

- [ ] **T7 — 120Hz + general-Android sweep (no-change verification)**
  Confirm: `enableHighRefreshRate()` present and called before content
  (`MainActivity.kt:83`), no `preferredDisplayMode=null` regressions, no
  hardcoded 60fps frame pacing in UI code (grep `Choreographer|postDelayed.*16|1000/60`),
  animations use vsync-friendly tween (already observed), `minSdk 28`
  guards around `display.refreshRate` (R+ API). Audio paths use sample-rate
  (not frame-rate) math — spot-check `AudioTrack` buffer sizing. Release
  flags present: `isMinifyEnabled/isShrinkResources` (release block),
  `resConfigs` state, `pickFirsts libonnxruntime.so` (duplicate .so guard).
  Truth: checklist with file:line refs, gaps filed as follow-ups (not fixed).

- [ ] **T8 — Repo bloat classification (tracked vs ignored, no deletions)**
  For each >100MB repo path (`ml/ 13G`, `IndicTrans2/ 6.5G` ignored,
  `android/build`, `raw/ 3.3G` ignored, `modelpacks/ 2G`, `santali_pdfs/
  1.8G`, `packages/ 1.6G`, `models/ 869M` ignored, `.git/ 5.3G`):
  report tracked-vs-ignored (`git check-ignore`), and for TRACKED heavies
  state keep-vs-untrack recommendation with reason (LFS? fetch-script?).
  Truth: table only. NOTHING deleted, untracked, or git-touched.

- [ ] **T9 — Write 11-VERIFICATION.md**
  Compile T1–T8 into: (a) measured budget table vs 500MB with gap math,
  (b) verdict register (every asset >1MB: LIVE/DEAD/NEEDS-DECISION + bytes
  + evidence + quality impact of removal), (c) ranked cut list summing to
  the gap WITHOUT touching scope (adapters, tokenizer prune, TTS int8, dex)
  vs WITH scope call (Mundari CT2 deferral ~240MB), (d) edge-case register
  (extraction doubling, 8GB tablets, restart resume, OOM guards).
  Truth: a reviewer can approve 11-02 execution row-by-row from this file.

</tasks>

<verification>
- `git status --short` shows ONLY `.planning/phases/11-apk-diet/11-VERIFICATION.md` as new (plus this PLAN.md + CONTEXT.md) — zero asset/code modifications. Enforce with `git diff --stat` showing no `android/` or model paths.
- Every DEAD verdict cites a negative-evidence grep command + output, re-runnable by the user.
- Every MB figure cites a measurement command (APK unzip listing, du, or onnx size), never an estimate without the PROJECTION label.
- 11-02 (execution) is explicitly NOT started.
</verification>

<success_criteria>
- [ ] Release tablet APK measured (or gap honestly labeled PROJECTION)
- [ ] Verdict register covers 100% of APK bytes >1MB
- [ ] Deletion shortlist sums to the <500MB gap with quality-impact notes
- [ ] Zero files deleted, moved, or edited outside `.planning/phases/11-apk-diet/`
- [ ] User can answer the Mundari scope question from the NEEDS-DECISION rows
</success_criteria>
