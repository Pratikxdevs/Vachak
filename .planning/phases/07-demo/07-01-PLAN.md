---
phase: 07-demo
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/demo-acceptance.md
  - scripts/demo_capture.sh
autonomous: false
requirements: ["DEMO-01"]
user_setup: ["2GB device, WiFi OFF, pack installed (P5), camera for screens"]

must_haves:
  truths:
    - "Full script passes WiFi OFF with zero hidden network (tcpdump/logcat clean)"
    - "Screens + Vachak-* logs + LatencyTracker + manifest captured"
    - "docs/demo-acceptance.md signed off, THIRD_PARTY_NOTICES complete"
  artifacts:
    - "docs/demo-acceptance.md"
    - "scripts/demo_capture.sh"
  key_links:
    - "Demo steps ↔ P1 MT + P2 TTS + P3 ASR + P4 curriculum + P6 <3s + P5 pack"
---

<objective>
Run WiFi-OFF demo acceptance and produce evidence doc.

Purpose: Final SIH gate — prove offline classroom flow end-to-end.
Output: Captured evidence + signed acceptance doc
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
@~/.claude/gsd-core/references/checkpoints.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/07-demo/07-CONTEXT.md
@AGENTS.md
@docs/PHASES.md
</context>

<threat_model>
STRIDE: Spoofing (fake offline — hidden hotspot), Information Disclosure (log leaks)
ASVS L1: Prove WiFi OFF (dumpsys wifi | grep mWifiEnabled), prove no INTERNET perm, no network calls in logcat
</threat_model>

<tasks>

<task type="auto">
  <name>Pre-condition: pack installed + no INTERNET + WiFi OFF</name>
  <files>scripts/demo_capture.sh</files>
  <read_first>AGENTS.md, android/app/src/main/AndroidManifest.xml</read_first>
  <action>Create scripts/demo_capture.sh that: checks adb shell dumpsys wifi | grep "Wi-Fi is disabled" before run; checks grep -r INTERNET android/ is zero; checks pack installed via adb shell run-as com.vachak ls files/packs/; starts logcat -s Vachak-MT Vachak-TTS Vachak-ASR Vachak-VAD Vachak-Assets Vachak-Latency -v time to file.</action>
  <verify>cat scripts/demo_capture.sh | grep -c "Wi-Fi is disabled" && cat android/app/src/main/AndroidManifest.xml | grep -c INTERNET || echo 0</verify>
  <acceptance_criteria>
    - "demo_capture.sh checks WiFi disabled and INTERNET absent"
    - "script starts logcat capture"
  </acceptance_criteria>
  <done>Pre-condition script ready</done>
</task>

<task type="checkpoint:human-verify">
  <name>Run full demo script WiFi OFF and capture screens + logs + latency</name>
  <files>docs/demo-acceptance.md</files>
  <read_first>AGENTS.md, docs/PHASES.md</read_first>
  <action>Human runs on 2GB device: 1) App opens → lesson loads 2) WiFi OFF (show setting) 3) Translate lesson → Ol Chiki shown 4) Play audio → Santali audible 5) Open worksheet → PDF renders 6) Open flashcard → image+text shown 7) Push-to-talk Hindi → recognized text shown → Santali translation → play audio 8) Diagnostics shows total <3s. After each step take screenshot + note LatencyTracker ms. Keep WiFi OFF entire time. Capture logcat dump + pack manifest + dumpsys meminfo. Do NOT turn on internet.</action>
  <verify>ls docs/demo-acceptance.md && grep -c "WiFi OFF" docs/demo-acceptance.md</verify>
  <acceptance_criteria>
    - "docs/demo-acceptance.md contains step table with pass/fail per step"
    - "Screenshots attached (docs/demo/screens/)"
  </acceptance_criteria>
  <done>Demo run captured, steps pass</done>
</task>

<task type="auto">
  <name>Write acceptance doc + provenance completeness check</name>
  <files>docs/demo-acceptance.md, THIRD_PARTY_NOTICES.md</files>
  <read_first>THIRD_PARTY_NOTICES.md, docs/MODEL_AND_DATA_PROVENANCE.md</read_first>
  <action>Write docs/demo-acceptance.md with: header (date, device, APK version, pack version, WiFi OFF proof), step table (step, expected, observed, evidence file, pass?), latency table (ASR/MT/TTS/total vs budget), storage/RSS table, provenance checklist (every repo/model/dataset license listed, voice consent attached), no-hidden-network attestation (logcat contains zero HttpURLConnection, dumpsys wifi disabled, manifest no INTERNET). Must be sign-off ready for SIH jury.</action>
  <verify>cat docs/demo-acceptance.md | grep -E "pass|WiFi OFF|Vachak-" | head -20 && wc -l docs/demo-acceptance.md</verify>
  <acceptance_criteria>
    - "docs/demo-acceptance.md contains latency and storage tables"
    - "THIRD_PARTY_NOTICES.md lists IndicTrans2, sherpa-onnx, ONNX Runtime, COILD, IndicVoices, Nirantar, Rasa"
  </acceptance_criteria>
  <done>Acceptance doc complete, provenance verified</done>
</task>

</tasks>

<artifacts>
Artifacts this phase produces:
- docs/demo-acceptance.md
- docs/demo/screens/*.png
- scripts/demo_capture.sh
</artifacts>
