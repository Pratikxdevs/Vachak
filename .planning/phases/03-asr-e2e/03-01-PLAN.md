---
phase: 03-asr-e2e
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - android/app/src/main/java/com/vachak/ui/MainScreen.kt
  - android/ml/src/main/java/com/vachak/ml/adapter/SherpaAsrAdapter.kt
  - android/ml/src/main/java/com/vachak/ml/VadStream.kt
  - android/ml/src/main/java/com/vachak/ml/LatencyTracker.kt
  - scripts/fetch_test_wav.py
autonomous: true
requirements: ["ASR-01", "ASR-02"]
user_setup: []

must_haves:
  truths:
    - "Push-to-talk returns Hindi transcript in ASR result (not mic err) on device with mic"
    - "WAV-fed path transcribes test file without mic on emulator/headless"
    - "VAD gates ASR and logs Vachak-ASR/Vad, ≤1s"
  artifacts:
    - "android/ml/src/main/java/com/vachak/ml/adapter/SherpaAsrAdapter.kt (bridged + logged)"
    - "android/app/src/main/java/com/vachak/ui/MainScreen.kt (shows transcript)"
  key_links:
    - "MainScreen PTT → VadStream → SherpaAsrAdapter.transcribe(ShortArray) → UI text"
---

<objective>
Fix ASR E2E (PCM16→VAD→whisper-tiny decode→UI) and add WAV fallback for emulator.

Purpose: Mic is stable but transcript not yet confirmed; need verifiable ASR without physical mic.
Output: Bridged adapter, WAV debug path, latency logging
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/03-asr-e2e/03-RESEARCH.md
@android/ml/src/main/java/com/vachak/ml/IndicConformerAsrAdapter.kt
@android/ml/src/main/java/com/vachak/ml/VadStream.kt
@android/app/src/main/java/com/vachak/ui/MainScreen.kt
@docs/PHASES.md
</context>

<threat_model>
STRIDE: Elevation (mic permission), Denial (buffer OOM)
ASVS L1: Validate RECORD_AUDIO granted before AudioRecord, validate WAV is 16k/mono/PCM16 before decode
</threat_model>

<tasks>

<task type="auto">
  <name>Fix Short→Float bridge and unify Vad/Tracker, surface transcript</name>
  <files>android/ml/src/main/java/com/vachak/ml/adapter/SherpaAsrAdapter.kt, android/ml/src/main/java/com/vachak/ml/VadStream.kt</files>
  <read_first>android/ml/src/main/java/com/vachak/ml/adapter/SherpaAsrAdapter.kt, android/app/src/main/java/com/vachak/ml/adapter/SherpaAsrAdapter.kt, android/ml/src/main/java/com/vachak/ml/LatencyTracker.kt</read_first>
  <action>Fix: :app SherpaAsrAdapter receives ShortArray from AudioRecord; :ml IndicConformerAsrAdapter expects FloatArray/32768 — add bridge conversion (short/32768f) and ensure OfflineRecognizer(null,config) is used (already fixed). Unify dual VadAnalyzer/VadDetector and dual LatencyTracker into single instance. In MainScreen push-to-talk flow (3s capture), feed through VadStream.isSpeech then decode, set ASR result Text with real Hindi string, log Vachak-ASR samples+latency, show "asr: <text> (<ms>ms)" in Status. Handle isFixture flag correctly.</action>
  <verify>grep -n "Vachak-ASR" android/ml/src/main/java/com/vachak/ml/adapter/SherpaAsrAdapter.kt android/ml/src/main/java/com/vachak/ml/IndicConformerAsrAdapter.kt && grep -n "ASR result" android/app/src/main/java/com/vachak/ui/MainScreen.kt</verify>
  <acceptance_criteria>
    - "SherpaAsrAdapter.kt contains 32768 bridge"
    - "MainScreen.kt shows ASR result from transcribe()"
  </acceptance_criteria>
  <done>Bridge fixed, transcript surfaced, logs present</done>
</task>

<task type="auto">
  <name>Add WAV-fed debug path (emulator/headless verifiable)</name>
  <files>android/app/src/main/java/com/vachak/ui/MainScreen.kt, scripts/fetch_test_wav.py</files>
  <read_first>android/app/src/main/java/com/vachak/ui/MainScreen.kt</read_first>
  <action>Add Debug button "Load WAV → ASR" (visible if BuildConfig.DEBUG) that loads a bundled Hindi WAV (16k/mono/PCM16, e.g. "नमस्ते" sample from datasets or scripts/fetch_test_wav.py), reads RIFF (validate ChunkID RIFF, fmt PCM16, 16k, mono), feeds ShortArray to same ASREngine.transcribe() entry point, shows result. Bundle test WAV under android/app/src/main/assets/test/hindi_sample.wav. This proves recognizer without mic.</action>
  <verify>ls android/app/src/main/assets/test/hindi_sample.wav && grep -n "Load WAV" android/app/src/main/java/com/vachak/ui/MainScreen.kt</verify>
  <acceptance_criteria>
    - "test wav exists 16k mono PCM16"
    - "MainScreen.kt contains Load WAV button feeding transcribe()"
  </acceptance_criteria>
  <done>WAV fallback works on emulator</done>
</task>

<task type="auto">
  <name>Latency gate ≤1s and permission guard</name>
  <files>android/app/src/main/java/com/vachak/ui/MainScreen.kt</files>
  <read_first>android/app/src/main/java/com/vachak/ui/MainScreen.kt</read_first>
  <action>Before AudioRecord, check ContextCompat.checkSelfPermission RECORD_AUDIO; if denied request permission (ActivityResult) else show "mic permission needed". Record LatencyTracker mark for ASR leg, assert ≤1000ms else warn in diagnostics. Keep sequential — do NOT run ASR parallel with MT/TTS.</action>
  <verify>grep -n "RECORD_AUDIO" android/app/src/main/java/com/vachak/ui/MainScreen.kt && grep -n "LatencyTracker" android/app/src/main/java/com/vachak/ui/MainScreen.kt | head</verify>
  <acceptance_criteria>
    - "MainScreen.kt checks RECORD_AUDIO before startRecording"
    - "LatencyTracker logs ASR ms"
  </acceptance_criteria>
  <done>Permission guard + latency gate present</done>
</task>

</tasks>

<artifacts>
Artifacts this phase produces:
- android/ml/src/main/java/com/vachak/ml/adapter/SherpaAsrAdapter.kt
- android/app/src/main/java/com/vachak/ui/MainScreen.kt (PTT + WAV)
- android/app/src/main/assets/test/hindi_sample.wav
</artifacts>
