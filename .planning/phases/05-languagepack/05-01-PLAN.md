---
phase: 05-languagepack
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/build_pack.py
  - android/sync/src/main/java/com/vachak/sync/PackInstaller.kt
  - android/sync/src/main/java/com/vachak/sync/PackManager.kt
  - android/app/src/main/java/com/vachak/ui/ManagePacksScreen.kt
  - android/app/src/main/AndroidManifest.xml
autonomous: true
requirements: ["PACK-01", "PACK-02"]
user_setup: []

must_haves:
  truths:
    - "packages/build_pack.py emits versioned .vachakpack with manifest + sha256 + license list"
    - "sync/ installs pack from storage, verifies hash, registers in Room, no network"
    - "Manage Packs UI lists installed packs, free space, install from file"
  artifacts:
    - "packages/build_pack.py"
    - "android/sync/src/main/java/com/vachak/sync/PackInstaller.kt"
  key_links:
    - "build_pack → .vachakpack → PackInstaller → app filesDir/packs → EngineProvider pack path"
---

<objective>
Build offline pack builder and sync/ installer with Manage Packs UI.

Purpose: Ship models+curriculum swappably without APK rebuild; enable P2 voice swap.
Output: Builder script, installer, pack-aware adapters, UI
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/05-languagepack/05-CONTEXT.md
@AGENTS.md
@packages/
@android/sync/
</context>

<threat_model>
STRIDE: Tampering (pack substitution), Spoofing (fake manifest), Elevation (zip slip)
ASVS L1: Verify sha256, validate manifest schema, sanitize zip paths (no ..), no INTERNET, limit pack size
Mitigations: hash verify, path sanitization, size guard, manifest signature placeholder
</threat_model>

<tasks>

<task type="auto">
  <name>Pack builder: bundle MT+TTS+ASR+curriculum → .vachakpack</name>
  <files>packages/build_pack.py, packages/manifest_schema.json</files>
  <read_first>packages/, AGENTS.md</read_first>
  <action>Build packages/build_pack.py that zips: vachak_models/mt/*, tts/* (model.onnx/tokens/lexicon), asr/*, curriculum/lessons/*.json + worksheet/templates + flashcard/assets + THIRD_PARTY_NOTICES.md into packs/sat_Olck-v{version}.vachakpack. Generate manifest.json {version, language:"sat_Olck", created, models:[{name,path,sha256,size}], curriculum:{sha256, lessonCount}, licenses:[{name,license,source}], totalBytes}. Compute sha256 per file + pack sha. Verify budget ~500MB total.</action>
  <verify>python packages/build_pack.py --version 0.1.0 && ls -lh packages/*.vachakpack && cat packages/manifest.json | grep sha256 | head</verify>
  <acceptance_criteria>
    - "packages/sat_Olck-v0.1.0.vachakpack exists"
    - "manifest.json contains sha256 and licenses"
  </acceptance_criteria>
  <done>Pack builder emits versioned pack with manifest</done>
</task>

<task type="auto">
  <name>Sync installer: verify + copy → filesDir/packs + Room register, no network</name>
  <files>android/sync/src/main/java/com/vachak/sync/PackInstaller.kt, android/sync/src/main/java/com/vachak/sync/PackManager.kt</files>
  <read_first>android/sync/src/main/java/com/vachak/sync/PackInstaller.kt</read_first>
  <action>Implement PackInstaller.install(uri:Uri): verifies manifest sha256 (recompute + compare), sanitizes zip entry paths (reject ..), copies to filesDir/packs/<version>/ via ContentResolver (no HttpURLConnection, no INTERNET), inserts Pack record in Room (id, version, language, path, installedAt), sets active pack. PackManager.getActivePack() returns path for adapters. Ensure AndroidManifest.xml has NO INTERNET permission — grep must be zero. Handle freeSpace check before copy.</action>
  <verify>grep -r "INTERNET" android/ --include="*.xml" | wc -l && grep -n "sha256\|PackInstaller" android/sync/src/main/java/com/vachak/sync/PackInstaller.kt | head</verify>
  <acceptance_criteria>
    - "INTERNET permission count == 0"
    - "PackInstaller.kt verifies sha256 and sanitizes zip paths"
  </acceptance_criteria>
  <done>Installer verifies and installs offline, no network</done>
</task>

<task type="auto">
  <name>Make adapters pack-aware + Manage Packs UI</name>
  <files>android/ml/src/main/java/com/vachak/ml/adapter/IndicTrans2Adapter.kt, android/ml/src/main/java/com/vachak/ml/SherpaOnnxTtsAdapter.kt, android/app/src/main/java/com/vachak/ui/ManagePacksScreen.kt</files>
  <read_first>android/ml/src/main/java/com/vachak/ml/adapter/IndicTrans2Adapter.kt, android/ml/src/main/java/com/vachak/ml/SherpaOnnxTtsAdapter.kt</read_first>
  <action>Modify IndicTrans2Adapter + SherpaOnnxTtsAdapter + SherpaAsrAdapter to resolve baseDir via PackManager.getActivePack() ?: SherpaAssets.prepare fallback. On pack install, engines must reload (close old ORT sessions, open new). Add ManagePacksScreen: lists installed packs (version, language, size, installed date), shows free space, button "Install from file" (Storage Access Framework), shows manifest licenses. Accessible from Settings or Diagnostics.</action>
  <verify>grep -n "PackManager\|activePack" android/ml/src/main/java/com/vachak/ml/adapter/IndicTrans2Adapter.kt && grep -n "ManagePacks" android/app/src/main/java/com/vachak/ui/*.kt</verify>
  <acceptance_criteria>
    - "Adapters read active pack path"
    - "ManagePacksScreen.kt lists packs"
  </acceptance_criteria>
  <done>Adapters pack-aware, Manage UI present, engines reload</done>
</task>

</tasks>

<artifacts>
Artifacts this phase produces:
- packages/build_pack.py + packs/*.vachakpack + manifest.json
- android/sync/src/main/java/com/vachak/sync/** (installer)
- android/app/src/main/java/com/vachak/ui/ManagePacksScreen.kt
</artifacts>
