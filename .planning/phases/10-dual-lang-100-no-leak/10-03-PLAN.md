# Plan 10-03: NavHost + LiveViewModel StateFlow + 2GB <3s + SME Gate (100%)

**Goal:** Full scalable architecture and device-proof closing remaining 20-25%.

**Tasks:**
1. **NavHost:** Replace `VachakApp:23 when(current)` with `NavHost` `NavController` `composable("home") { HomeScreen(viewModel: hiltViewModel()) }` etc. Type-safe `NavDest` `@Serializable` with args `lessonId`. Migrate `pendingLesson` to `NavBackStackEntry savedStateHandle`. `AdaptiveScaffold` consumes `WindowSizeClass` not `BoxWithConstraints`. Keep `isTablet` logic.
2. **LiveViewModel StateFlow:** Replace God `LiveScreen 676` lines: `LiveViewModel: ViewModel` with `UiState(data class isListening, partialText, committedText, santaliText, latencyMs, error)` `StateFlow` `WhileSubscribed(5000)`, intents `onStart/onStop/onTextChange/onRetry`. Move `debounce 600`, `AudioCapturer`, `StreamingAsrSession`, `LatencyTracker`, `ensureAdapterExtracted` to VM `viewModelScope`. Composable becomes `~150` lines collecting `uiState`. `AudioCapturer cap 14s` + `sequenced Dispatchers.IO.limitedParallelism(1)` + `Mutex` for `ASR->MT->TTS` never parallel.
3. **Device <3s capture:** Run on `2GB Android9 arm64` target: `adb logcat -c; adb shell dumpsys wifi disable; adb logcat -s Vachak-Latency -s Vachak-MT --format=json > /tmp/run.json` then `python benchmarks/run_benchmark.py --mode=device --device-id <id>` to fill `docs/benchmarks/BENCHMARK_REPORT.md` `p50/p95` from `elapsedRealtimeNanos` (forbid_dev_as_android). Script `scripts/demo_capture.sh` records `adb logcat -s Vachak-*` + `dumpsys meminfo` + screenshots for `docs/demo-acceptance.md`.
4. **SME gate:** `curriculum/lessons/sat_lessons.json` stays `AUTHOR-DRAFT` `DRAFT` watermark `curriculum/data.py validate_warnings` -> `2.2% verified` kept `DRAFT` until native speaker review. Pack `build_pack --require-approved` fails if `MACHINE_TRANSLATED` would be `APPROVED`. Update `docs/MODEL_AND_DATA_PROVENANCE.md` provenance.

**Verification:**
- `./gradlew assembleDebug` + `assembleRelease` (R8) both PASS, `apkanalyzer` `com.vachak` `arm64` only, `grep -r "when(current)" VachakApp.kt` ==0, `grep -r "allowMainThreadQueries" android/content` only debug.
- `adb logcat --pid=$(pidof com.vachak) | grep Latency` shows `VOICE END total 136ms <3s` on `2GB` device, `BENCHMARK_REPORT.md` `Measured` filled not `PENDING`.
- `grep -c provenance curriculum/lessons/sat_lessons.json` >=15, `pack --require-approved` fails on `DRAFT` as expected.
- Manual WiFi OFF demo script `docs/DEMO.md 110s` passes: open -> lesson -> WiFi OFF -> translate -> play -> worksheet -> flashcards -> PTT -> Santali -> audio -> diagnostics.

**Files:** `VachakApp.kt`, `NavDest.kt`, `LiveViewModel.kt`, `LiveScreen.kt`, `AppDatabase.kt`, `BENCHMARK_REPORT.md`, `demo-acceptance.md`

**Estimate:** 1.5 days, no deletion.
