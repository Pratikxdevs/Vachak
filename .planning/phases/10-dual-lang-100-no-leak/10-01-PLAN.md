# Plan 10-01: Dual-Language Adapter + Debug UI (No Leak)

**Goal:** Every UI page supports Santali + Mundari as adapters with no hard-coded language, plus debug UI.

**Tasks:**
1. **Adapter switcher:** Refactor `EngineProvider.real()` `activeLang` to `StateFlow<String>` via `PackManager.getActivePack()`, expose `LanguageSwitcher` composable in `VachakApp` top bar + `SettingsScreen` dropdown `Santali (Ol Chiki) <-> Mundari`. Remove hard-coded `LanguagePair("hi","unr_Deva")` in `LiveScreen 166/516`, `ToolsScreen`, `HomeScreen`. Use `engine.packs.getActivePackFor(activeLang)` resolved path.
2. **All pages:** Audit `HomeScreen 101 CurriculumScreen 80 ToolsScreen` - replace `title.contains("language")` with `LessonFilter(domain)` already done, ensure `translate`, `playPcm(lang)`, `WorksheetPane`, `FlashcardPane` respect `activeLang`. Add `sat_Olck` / `unr_Deva` `OlChikiValidator` tick per language.
3. **Debug UI:** Enhance `DiagnosticsScreen 35` live `storageUsedBytes()` + `freeSpace` + `activeAdapter` badge; new `DebugOverlay.kt` floating `LazyColumn` with `LogcatWatcher` `Vachak-*` (offline, in-proc ring buffer 200 lines) toggle in `SettingsScreen`. Add `ManagePacksScreen` adapter badge `13M` + `packSha256` + `withinBudget`.
4. **No leak verification:** `grep -r "sat_Olck" android/app/src/main/java --include="*.kt" | grep -v "LanguagePair.*sat_Olck"` must be 0 after; every `translate` goes via `activeLang`.

**Verification:**
- `grep -r "unr_Deva\|sat_Olck" android/app/src/main/java | wc -l` before/after down to only `EngineProvider` + `AdapterTranslationEngine` adapterMap.
- Manual: Launch app, switch `Settings -> Language Mundari`, `Home` shows Mundari lesson titles, `Live` PTT `हैलो` -> `जोहार` (refMap or merged), switch back `Santali` -> `Curriculum` flashcard Ol Chiki `U+1C50`.
- `adb logcat -s Vachak-MT | grep AdapterEngine` shows `present=true` for both langs.

**Files:** `VachakApp.kt`, `EngineProvider.kt`, `LiveScreen.kt`, `CurriculumScreen.kt`, `ToolsScreen.kt`, `SettingsScreen.kt`, `DiagnosticsScreen.kt`, `DebugOverlay.kt` (new), `AdapterTranslationEngine.kt`

**Estimate:** 1.5 days, no deletion.
