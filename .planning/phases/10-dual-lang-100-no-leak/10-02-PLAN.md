# Plan 10-02: Real Voice + LoRA Merge (ML 100%)

**Goal:** Replace shim/placeholder ML with real trained artifacts, keep shim for CI, no delete ONNX until CT2 passes arm64.

**Tasks:**
1. **LoRA merge:** Run `ml/translation/scripts/merge_lora_to_ct2.py --base models/indictrans2_bart --adapter ml/finetune/it2_mundari_lora_real --out /tmp/merged --convert` (requires `peft`). If GPU absent, keep `--dry-run` placeholder + document `stripped_mt_merged/README.md`. On GPU, produce `modelpacks/stripped_mt_merged/model.bin 221M` + `shared_vocabulary 93k`. Benchmark `benchmarks/ct2_migration_benchmark.py` must still `cold<2000 warm<500 BLEU>5` (currently `493/26/22.1`). Keep `android/ml/src/main/assets/vachak_models/mt 356M` untouched.
2. **TTS real:** Train `ml/tts/train.py --real --config ml/tts/configs/sat_vits.json` on `IndicVoices SAT-IV-SP001 3200/4.52h` (22.05k, Ol Chiki `U+1C50` 55 tokens) Coqui `300ep` `AdamW 2e-4` `mixed_precision`. If GPU absent, keep shim but enhance `SherpaOnnxTtsAdapter 43 detectShim` to log `isFixture` and `DiagnosticsScreen` to show `MOS PENDING` not `3.5`. Export `models/vits-sat.onnx` `opset17` `tokens.txt` `lexicon.txt` via `export_onnx.py` -> `android/app/src/main/assets/vachak_models/tts` (keep shim as `tts/shim/` for CI). Ensure `VOICE_CONSENT` unsigned but `THIRD_PARTY` provenance intact.
3. **Guard:** `AdapterTranslationEngine` `translate` TODO comment references `merge_lora_to_ct2.py` and `refMap` fallback for demo; add `BuildConfig.DEBUG` gate to hide `refMap` in `release` (return `Err` not mock).

**Verification:**
- `ls modelpacks/stripped_mt_merged/model.bin` exists or dry-run log `DRY RUN`.
- `ml/export_venv/bin/python benchmarks/ct2_migration_benchmark.py` still PASS, `ONNX_MODEL` untoched.
- On device: `Live PTT` `हैलो` -> `जोहार` via merged CT2 (not `[CT2-MUNDARI]`), `TTS` `playPcm` `>4410 samples` audible + `Vachak-TTS` not `SHIM`.
- `grep -r "refMap" android/ml --include="*.kt"` only under `BuildConfig.DEBUG`.

**Files:** `ml/translation/scripts/merge_lora_to_ct2.py`, `modelpacks/stripped_mt_merged/*`, `ml/tts/train.py`, `android/ml/.../SherpaOnnxTtsAdapter.kt`, `AdapterTranslationEngine.kt`

**Estimate:** 2 days (1 GPU day if available, else dry-run + docs).
