---
phase: 08-live-wiring
plan: "03"
type: execute
wave: 3
depends_on: ["08-02"]
files_modified:
  - android/app/src/main/java/com/vachak/ui/screens/LiveScreen.kt
  - android/ml/src/main/java/com/vachak/ml/SherpaOnnxTtsAdapter.kt
  - android/app/src/main/java/com/vachak/ui/screens/SettingsScreen.kt
autonomous: true
requirements: ["TTS-01", "TTS-02", "PERF-01"]
user_setup: []

must_haves:
  truths:
    - "Play Audio synthesizes Santali Ol Chiki via sherpa-onnx VITS at 22050Hz and plays via AudioTrack audible >200ms (not 16000 hardcoded, not silent)"
    - "Full pipeline ASR→MT→TTS is sequential (never parallel), T0→T4 total <3s logged via LatencyTracker and warns if over"
    - "Engines reload from active pack path (pack-aware) and no INTERNET permission exists"
  artifacts:
    - "android/app/src/main/java/com/vachak/ui/screens/LiveScreen.kt (sequential pipeline + TTS playback)"
  key_links:
    - "santaliText (MT) → SherpaOnnxTtsAdapter.synthesize(sat,'sat_Olck') → SynthAudio(sampleRate=22050) → AudioTrack(MUSIC, sampleRate) → audible"
---

<objective>
Wire TTS playback and full sequential orchestration (ASR→MT→TTS) with latency/pack/offline.

Purpose: Complete live MVP by wiring Santali VITS synthesis at correct sampleRate + AudioTrack audible check, enforcing sequential `numThreads=1` + `ReentrantLock` via `Orchestrator` or LiveScreen coroutine, marking LatencyTracker T3→T4, and exposing diagnostics. This makes speaking Hindi → live transcription → Santali translation → Santali audio work end-to-end.
Output: Audible Santali audio >200ms at 22050Hz, sequential lock, T0→T4 <3s Vachak-* logs, pack-aware reload, offline badge
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/08-live-wiring/08-CONTEXT.md
@.planning/phases/08-live-wiring/08-RESEARCH.md
@android/app/src/main/java/com/vachak/ui/screens/LiveScreen.kt
@android/ml/src/main/java/com/vachak/ml/SherpaOnnxTtsAdapter.kt
@android/app/src/main/java/com/vachak/ml/adapter/SherpaTtsAdapter.kt
@android/ml/src/main/java/com/vachak/ml/LatencyTracker.kt
@android/sync/src/main/java/com/vachak/sync/PackManager.kt
@android/app/src/main/java/com/vachak/ui/screens/SettingsScreen.kt
@android/core/src/main/java/com/vachak/engine/EngineContracts.kt
@android/ml/orchestrator/Orchestrator.kt
</context>

<threat_model>
STRIDE: Tampering (model swap), Information Disclosure (Ol Chiki text logged), Denial (TTS OOM if not released), Elevation (AudioTrack not permission-gated)
ASVS L1: Validate sat_Olck Ol Chiki range, check pcm.size >0.2*sampleRate before play, sanitize zip not relevant, no INTERNET
Mitigations: OlChikiValidator, audible size guard, sequential TTS dataDir="" (no espeak GPL), pack-aware fallback, sequential lock, no network
</threat_model>

<tasks>

<task type="auto">
  <name>Fix TTS playback to 22050Hz audible >200ms via SherpaOnnxTtsAdapter sat_Olck</name>
  <files>android/app/src/main/java/com/vachak/ui/screens/LiveScreen.kt</files>
  <read_first>android/app/src/main/java/com/vachak/ui/screens/LiveScreen.kt, android/ml/src/main/java/com/vachak/ml/SherpaOnnxTtsAdapter.kt, android/app/src/main/java/com/vachak/ml/adapter/SherpaTtsAdapter.kt</read_first>
  <action>In LiveScreen after MT `santaliText` is set: Call `val pcmRes = engine.tts.synthesize(santaliText, "sat_Olck")` (not "mund" — keep mund alias in `SherpaTtsAdapter.supports` but UI primary is sat_Olck). Handle `EngineResult`: on `Ok(SynthAudio(samples,sampleRate))` check `samples.size > 0.2*sampleRate` (4410 @22050 / 4800 @24000), log `Vachak-TTS synthesized "$sat" -> ${samples.size} samples @ ${sampleRate}Hz` + `audible check PASS` or `WARN <200ms`; play via `tryPlayPcm` refactored to `tryPlayPcm(pcm: ShortArray, sampleRate:Int)` using `AudioTrack(AudioManager.STREAM_MUSIC, sampleRate, CHANNEL_OUT_MONO, PCM_16BIT, pcm.size*2, MODE_STATIC)` `write→play→sleep(pcm.size*1000/sampleRate)→stop/release` (remove hardcoded `16000`); on `Err` show snack `TTS: ${msg}`. Keep `engine` pack-aware: `SherpaOnnxTtsAdapter.resolveBaseDir` already checks `PackManager.getActivePackFor("tts")` / `vachak_models/tts` else `SherpaAssets.prepare`; ensure `reloadFromPack()` is callable on pack switch (call after install or detect baseDir change). Log `Vachak-TTS` at synthesize + audible check. Update `Play Audio` button to use `santaliText` primary, `hindiText` fallback disabled if empty.</action>
  <verify>grep -n "synthesize.*sat_Olck\|Vachak-TTS" android/app/src/main/java/com/vachak/ui/screens/LiveScreen.kt | head && grep -n "22050\|sampleRate\|0.2" android/app/src/main/java/com/vachak/ui/screens/LiveScreen.kt | head</verify>
  <acceptance_criteria>
    - "LiveScreen.kt synthesizes with sat_Olck (not mund primary) and logs Vachak-TTS"
    - "AudioTrack uses pcm sampleRate (22050) not hardcoded 16000"
    - "Audible check >0.2*sampleRate present"
  </acceptance_criteria>
  <done>TTS at 22050Hz audible >200ms, sat_Olck, logged</done>
</task>

<task type="auto">
  <name>Enforce sequential ASR→MT→TTS + LatencyTracker T3→T4 <3s + diagnostics/pack/offline</name>
  <files>android/app/src/main/java/com/vachak/ui/screens/LiveScreen.kt, android/app/src/main/java/com/vachak/ui/screens/SettingsScreen.kt</files>
  <read_first>android/app/src/main/java/com/vachak/ui/screens/LiveScreen.kt, android/ml/src/main/java/com/vachak/ml/LatencyTracker.kt, android/ml/orchestrator/Orchestrator.kt</read_first>
  <action>In LiveScreen refactor `runPipeline` to single `scope.launch(Dispatchers.IO)` sequential block: hold `isTranslating=true` guard at top (early return if already translating), then `withContext(Dispatchers.IO)` sequential `val asr=transcribe`, `val mt=translate`, `val tts=synthesize` + `tryPlayPcm` — never `launch` parallel, never hold two models resident (numThreads=1 enforced in `OfflineRecognizerConfig`/`OfflineTtsConfig` + `IndicTrans2Adapter ReentrantLock`). Use the `ml/LatencyTracker` started in 08-01's `markSpeechBegin` → `markAsr` (08-01) → `markTranslate` (08-02) → `markTtsBegin` before `synthesize` → `markAudioBegin` after `track.play()`; log `Vachak-Latency total=${tracker.result().endToEndMs()} stageMs=${tracker.result().stageMs()} withinBudget=${<3000}` and warn `LATENCY_WARN total>3000` + per-stage `asr>1000|mt>500|tts>1000`; show badge `Pipeline: ${ms}ms <3s ✓` vs `≥3s ⚠` already in LiveScreen but ensure it uses `tracker.result()` not `currentTimeMillis`. After synthesis, set `isTranslating=false` on Main dispatcher. In `SettingsScreen` ensure diagnostics `Benchmark` section reads `engine.benchmark` or `LatencyTracker` last result and shows `ASR≤1s MT≤0.5 TTS≤1 <3s sequential`; keep `Manage Packs` button (already wired) showing `PackManager.getActivePack` and free space. Verify offline: `grep -r android.permission.INTERNET android/` must be 0 (only `RECORD_AUDIO` + `READ_EXTERNAL_STORAGE`), no `HttpURLConnection` in `ml/`/`sync/` runtime (only test comments). Pack-aware reload: if pack installed, adapters `resolveBaseDir` change triggers `closeSessions`/`reloadFromPack` — LiveScreen does not need manual reload, but after `ManagePacksScreen` `setActive`, next pipeline uses new pack.</action>
  <verify>grep -n "LatencyTracker\|Vachak-Latency\|markTts\|markAudio" android/app/src/main/java/com/vachak/ui/screens/LiveScreen.kt | head -n 10 && grep -n "isTranslating\|Sequential\|ReentrantLock" android/app/src/main/java/com/vachak/ui/screens/LiveScreen.kt | head</verify>
  <acceptance_criteria>
    - "LiveScreen.kt sequential single coroutine with isTranslating guard, no parallel launch of ASR/MT/TTS"
    - "LiveScreen.kt marks T3 TTS and T4 audio and logs Vachak-Latency total <3s"
    - "SettingsScreen still shows Manage Packs and benchmark ASR≤1s MT≤0.5 TTS≤1"
    - "No INTERNET permission in AndroidManifest"
  </acceptance_criteria>
  <done>Sequential pipeline locked, T0→T4 <3s logged, pack-aware/offline</done>
</task>

</tasks>

<artifacts>
Artifacts this phase produces:
- android/app/src/main/java/com/vachak/ui/screens/LiveScreen.kt (full ASR→MT→TTS sequential + 22050Hz audible + T0→T4)
- android/ml/src/main/java/com/vachak/ml/SherpaOnnxTtsAdapter.kt (verified pack-aware reload, 22050 audible)
- android/app/src/main/java/com/vachak/ui/screens/SettingsScreen.kt (diagnostics Manage Packs + benchmark)
</artifacts>
