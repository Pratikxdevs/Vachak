---
phase: 08-live-wiring
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - android/app/src/main/java/com/vachak/ui/screens/LiveScreen.kt
  - android/app/src/main/java/com/vachak/ml/adapter/AudioPipeline.kt
  - android/app/src/main/AndroidManifest.xml
autonomous: true
requirements: ["ASR-01", "ASR-02"]
user_setup: []

must_haves:
  truths:
    - "FAB hold-to-talk captures 16k mono PCM16 via AudioRecord and shows real Hindi transcription in LiveScreen DualLangCard (not [DEV-FIXTURE] or simulated string)"
    - "VAD gates ASR (silero_vad.onnx) and silence shows hint, not crash; logs Vachak-VAD and Vachak-ASR"
    - "RECORD_AUDIO permission is requested via launcher before capture; denied shows mic permission needed"
  artifacts:
    - "android/app/src/main/java/com/vachak/ui/screens/LiveScreen.kt (wired FAB → AudioRecord → VAD → SherpaAsrAdapter)"
  key_links:
    - "LiveScreen FAB → AudioRecord(16000,MONO,PCM16) → VadAdapter.SherpaVadDetector → SherpaAsrAdapter.transcribe(ShortArray) → hindiText state → DualLangCard"
---

<objective>
Wire LiveScreen audio capture to real Hindi ASR so speaking shows live transcription.

Purpose: Replace LiveScreen mock `simulatedHindi + delay(180)` with real `AudioRecord(16k mono PCM16 4s max) → VAD → SherpaAsrAdapter.transcribe` and surface Hindi in DualLangCard. This is the first half of live MVP (capture+ASR) and unblocks MT live translation.
Output: Real hold-to-talk FAB, permission gate, PCM16→VAD→ASR decode, Hindi transcription UI + Vachak-ASR/VAD logs + LatencyTracker T0→T1 ≤1s
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/08-live-wiring/08-CONTEXT.md
@.planning/phases/08-live-wiring/08-RESEARCH.md
@android/app/src/main/java/com/vachak/ui/screens/LiveScreen.kt
@android/app/src/main/java/com/vachak/ui/MainScreen.kt
@android/app/src/main/java/com/vachak/ml/adapter/AudioPipeline.kt
@android/ml/src/main/java/com/vachak/ml/VadStream.kt
@android/app/src/main/java/com/vachak/ml/adapter/VadAdapter.kt
@android/ml/src/main/java/com/vachak/ml/IndicConformerAsrAdapter.kt
@android/app/src/main/java/com/vachak/ml/adapter/SherpaAsrAdapter.kt
@android/core/src/main/java/com/vachak/engine/EngineContracts.kt
@android/ml/src/main/java/com/vachak/ml/LatencyTracker.kt
@android/app/src/main/java/com/vachak/engine/EngineProvider.kt
</context>

<threat_model>
STRIDE: Spoofing (fake Hindi injection via wav), Tampering (zip slip not relevant), Elevation (mic permission), Denial (buffer OOM, 4s cap), Information Disclosure (recording leaks via logs)
ASVS L1: Verify RECORD_AUDIO granted before AudioRecord, validate sampleRate==16000 mono PCM16 before transcribe, no INTERNET permission, log redaction (do not log raw PCM)
Mitigations: permission launcher, 4s max ShortArray, VAD drop silence, LatencyTracker monotonic, no INTERNET, pack-aware fallback
</threat_model>

<tasks>

<task type="auto">
  <name>Add AudioRecord capture + RECORD_AUDIO launcher + VAD wiring to LiveScreen FAB</name>
  <files>android/app/src/main/java/com/vachak/ui/screens/LiveScreen.kt, android/app/src/main/java/com/vachak/ml/adapter/AudioPipeline.kt</files>
  <read_first>android/app/src/main/java/com/vachak/ui/screens/LiveScreen.kt, android/app/src/main/java/com/vachak/ui/MainScreen.kt, android/app/src/main/java/com/vachak/ml/adapter/AudioPipeline.kt, android/ml/src/main/java/com/vachak/ml/VadStream.kt, android/app/src/main/java/com/vachak/ml/adapter/VadAdapter.kt</read_first>
  <action>Replace LiveScreen mock `runPipeline(simulatedHindi: String = "नमस्ते…")` + `delay(180)` with real capture: FAB tap toggles `isListening`; on `toggleListening() -> true` clear `hindiText/santaliText/latency`, on `false` (stop) launch `Dispatchers.IO` coroutine that: (a) checks `ContextCompat.checkSelfPermission(RECORD_AUDIO)`, if denied launch `rememberLauncherForActivityResult(RequestPermission)` and show `mic permission needed` snackbar and return; (b) creates `LatencyTracker(LatencySample("live-${elapsedRealtimeNanos()}"))` and `markSpeechBegin()`; (c) calls `recordMic(sampleRate=16000, maxMs=4000)` (port from `MainScreen.kt:220` `AudioRecord(MIC,16000,MONO,PCM16)` `getMinBufferSize` + `maxOf(minBuf,sampleRate*2)` + `while(RECORDSTATE_RECORDING)` + `read` + `stop/release` → `ShortArray`) OR fleshes `AudioPipeline.captureAndRecognize` to do same and returns `ShortArray` (keep one implementation, delete fixture `ShortArray(frameCount)`); (d) logs `Vachak-ASR recordMic captured ${pcm.size} samples`; (e) calls `engine.asr.transcribe(pcm,16000)` via `SherpaAsrAdapter` (which internally `vad.detect` SherpaVadDetector → segments → Short/32768.0f → `IndicConformerAsrAdapter` `OfflineRecognizer(null,config)` whispertiny HI, pack-aware `PackManager.getActivePackFor("asr")`). Flesh `AudioPipeline` if used: implement real `AudioRecord.read` loop, remove fixture silent buffer. Keep `tryPlayPcm` unchanged in this plan (TTS deferred to 08-03). Ensure `AndroidManifest.xml` still has `RECORD_AUDIO` and no `INTERNET` (grep 0).</action>
  <verify>grep -n "AudioRecord" android/app/src/main/java/com/vachak/ui/screens/LiveScreen.kt && grep -n "RECORD_AUDIO" android/app/src/main/java/com/vachak/ui/screens/LiveScreen.kt && grep -n "Vachak-ASR\|Vachak-VAD" android/app/src/main/java/com/vachak/ui/screens/LiveScreen.kt | head</verify>
  <acceptance_criteria>
    - "LiveScreen.kt contains AudioRecord(MIC, 16000"
    - "LiveScreen.kt checks RECORD_AUDIO via ContextCompat or permission launcher before AudioRecord"
    - "LiveScreen.kt calls engine.asr.transcribe(pcm"
    - "No delay(180) MT mock remains as primary path (real ASR is gated before MT)"
  </acceptance_criteria>
  <done>LiveScreen FAB triggers real mic capture → VAD → ASR</done>
</task>

<task type="auto">
  <name>Surface live Hindi transcription in DualLangCard + LatencyTracker T0→T1 + errors</name>
  <files>android/app/src/main/java/com/vachak/ui/screens/LiveScreen.kt</files>
  <read_first>android/app/src/main/java/com/vachak/ui/screens/LiveScreen.kt, android/ml/src/main/java/com/vachak/ml/LatencyTracker.kt</read_first>
  <action>In same LiveScreen coroutine after `transcribe`: handle `EngineResult`: on `Ok(text)` set `hindiText = text` (real Hindi, not fixture), `statusLine = "asr: $text (${ms}ms)"`, `tracker.markAsr(text)` and log `Vachak-ASR transcribed ${pcm.size} samples -> "$text" (${ms}ms)` + `Vachak-Latency ASR ${ms}ms withinBudget=${ms<=1000}` and warn if `>1000`; on `Err` set `hindiText = "ERR $code: $msg"` + snackbar `mic err: $msg`; if `text.isBlank()` (VAD silence `segments empty → ""`) set hint `silence — try again, speak closer`; update `DualLangCard(label="Hindi (ASR)", text=hindiText)` live; set `isTranslating=false` after ASR so FAB re-enables; keep `santaliText` cleared until 08-02 MT wiring; ensure `LatencyTracker` is `ml/LatencyTracker` with `elapsedRealtimeNanos` (not `currentTimeMillis`), and import `com.vachak.ml.LatencyTracker` + `LatencySample`. Remove simulatedHindi default param from `runPipeline` (or keep overloaded but real path is primary).</action>
  <verify>grep -n 'DualLangCard(label="Hindi' android/app/src/main/java/com/vachak/ui/screens/LiveScreen.kt && grep -n "LatencyTracker\|Vachak-Latency\|Vachak-ASR" android/app/src/main/java/com/vachak/ui/screens/LiveScreen.kt | head -n 10</verify>
  <acceptance_criteria>
    - "LiveScreen.kt updates hindiText from transcribe Ok value and shows in DualLangCard Hindi (ASR)"
    - "LiveScreen.kt imports com.vachak.ml.LatencyTracker and logs Vachak-Latency/Vachak-ASR"
    - "Silence (empty VAD) handled without crash and shows hint"
  </acceptance_criteria>
  <done>Live Hindi transcription surfaced, permission/VAD/latency gated</done>
</task>

</tasks>

<artifacts>
Artifacts this phase produces:
- android/app/src/main/java/com/vachak/ui/screens/LiveScreen.kt (FAB → AudioRecord → VAD → ASR → hindiText)
- android/app/src/main/java/com/vachak/ml/adapter/AudioPipeline.kt (real captureAndRecognize, not fixture silent buffer)
</artifacts>
