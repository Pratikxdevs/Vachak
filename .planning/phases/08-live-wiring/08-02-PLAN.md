---
phase: 08-live-wiring
plan: "02"
type: execute
wave: 2
depends_on: ["08-01"]
files_modified:
  - android/app/src/main/java/com/vachak/ui/screens/LiveScreen.kt
  - android/ml/src/main/java/com/vachak/ml/adapter/IndicTrans2Adapter.kt
autonomous: true
requirements: ["MT-02", "MT-03"]
user_setup: []

must_haves:
  truths:
    - "Tapping Translate after live Hindi transcription calls real IndicTrans2 Hin→sat_Olck and shows Santali Ol Chiki in second DualLangCard with Ol Chiki validation"
    - "MT logs Vachak-MT and marks LatencyTracker T2 within ≤500ms; no simulated delay"
    - "LanguagePair uses sat_Olck (not mund) and supports pack-aware reload"
  artifacts:
    - "android/app/src/main/java/com/vachak/ui/screens/LiveScreen.kt (MT live wiring)"
  key_links:
    - "hindiText (ASR) → IndicTrans2Adapter.translate(text, LanguagePair('hi','sat_Olck')) → santaliText → DualLangCard Sat Ol Chiki"
---

<objective>
Wire live MT translation (Hin→Santali Ol Chiki) to LiveScreen second pane.

Purpose: Replace LiveScreen `delay(180)` + `mund` mock with real `IndicTrans2Adapter.translate` (`sat_Olck` Ol Chiki, pack-aware, sequential lock) and surface Santali in DualLangCard with validation tick. Enables live translation half of MVP.
Output: Real Hin→sat_Olck MT call, Ol Chiki pane, Vachak-MT log + T2 latency, pack-aware
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/08-live-wiring/08-CONTEXT.md
@.planning/phases/08-live-wiring/08-RESEARCH.md
@android/app/src/main/java/com/vachak/ui/screens/LiveScreen.kt
@android/ml/src/main/java/com/vachak/ml/adapter/IndicTrans2Adapter.kt
@android/ml/src/main/java/com/vachak/ml/IndicProcessorPort.kt
@android/ml/src/main/java/com/vachak/ml/LatencyTracker.kt
@android/core/src/main/java/com/vachak/engine/EngineContracts.kt
@android/app/src/main/java/com/vachak/engine/EngineProvider.kt
@android/sync/src/main/java/com/vachak/sync/PackManager.kt
</context>

<threat_model>
STRIDE: Spoofing (fake sat_Olck tag), Tampering (model swap), Elevation (reflection via tokenizer JSON)
ASVS L1: Validate language codes (hi/sat_Olck whitelist), clamp token IDs (<dict_size else unk_id), validate Ol Chiki range U+1C50–U+1C7F before UI, no INTERNET
Mitigations: supports() whitelist, id clamp, OlChikiValidator regex, no reflection, pack-aware but still fallback, ReentrantLock sequential
</threat_model>

<tasks>

<task type="auto">
  <name>Replace mock translate with real IndicTrans2 Hin→sat_Olck + Ol Chiki validation</name>
  <files>android/app/src/main/java/com/vachak/ui/screens/LiveScreen.kt</files>
  <read_first>android/app/src/main/java/com/vachak/ui/screens/LiveScreen.kt, android/ml/src/main/java/com/vachak/ml/adapter/IndicTrans2Adapter.kt, android/ml/src/main/java/com/vachak/ml/IndicProcessorPort.kt</read_first>
  <action>In LiveScreen `runPipeline` after ASR `hindiText` is set (from 08-01's `transcribe`): remove `delay(180)` + `LanguagePair("hi","mund")` mock; call `val mt = engine.translation.translate(asrText, LanguagePair("hi","sat_Olck"))` (keep `mund` alias supported via `supports()` but UI must show `sat_Olck`); handle `EngineResult`: on `Ok(sat)` validate Ol Chiki via `sat.any{it.code in 0x1C50..0x1C7F}` (match `IndicTrans2Adapter.kt:129` regex `[\u1C50-\u1C7F]`), log `Vachak-MT input->sat` and warning `OlChikiValidator: no OlChiki` if missing, set `santaliText = sat` and show tick badge; on `Err` set `santaliText = "[MT unavailable] ${e.message}"` + snackbar. Keep `engine` as `EngineProvider.real(context)` (pack-aware via `PackManager.getActivePackFor("mt")` inside `IndicTrans2Adapter.resolveBaseDir` + `closeSessions` on pack switch); do not hardcode asset path. Remove simulatedHindi default — real `hindiText` from 08-01 is only source (keep typed `OutlinedTextField` as fallback input for no-mic debug, but primary is live ASR).</action>
  <verify>grep -n 'LanguagePair.*sat_Olck' android/app/src/main/java/com/vachak/ui/screens/LiveScreen.kt && grep -n "Vachak-MT\|OlChikiValidator\|sat_Olck" android/app/src/main/java/com/vachak/ui/screens/LiveScreen.kt | head && grep -n "delay(180)" android/app/src/main/java/com/vachak/ui/screens/LiveScreen.kt | wc -l</verify>
  <acceptance_criteria>
    - "LiveScreen.kt calls translate with sat_Olck (not mund as primary)"
    - "LiveScreen.kt logs Vachak-MT and validates Ol Chiki range"
    - "delay(180) mock count == 0 (real MT, not simulated)"
  </acceptance_criteria>
  <done>Live MT real Hin→sat_Olck, Ol Chiki pane shows Santali</done>
</task>

<task type="auto">
  <name>Mark LatencyTracker T2 translate ≤500ms and log Vachak-Latency</name>
  <files>android/app/src/main/java/com/vachak/ui/screens/LiveScreen.kt</files>
  <read_first>android/app/src/main/java/com/vachak/ui/screens/LiveScreen.kt, android/ml/src/main/java/com/vachak/ml/LatencyTracker.kt</read_first>
  <action>In same coroutine, use the `ml/LatencyTracker` started in 08-01's `markSpeechBegin`/`markAsr`: after ASR `markAsr(hindi)`, before MT `tracker.markTranslate(sat)` is not correct — instead use `tracker.markTranslate(sat)` after `translate` returns (as in `IndicTrans2Adapter.kt:103` `markTranslate(postprocessed)` pattern) or `tracker.markTranslate` via `LatencyTracker` T2; ensure import `com.vachak.ml.LatencyTracker` + `LatencySample` `elapsedRealtimeNanos` (not `currentTimeMillis`), log `Vachak-Latency translate ${tracker.result().stageMs()["translate"]}ms withinBudget=${<=500}` and `Vachak-MT latency ${latencyMs}ms`; warn `LATENCY_WARN >500ms` if exceeded; keep sequential `ReentrantLock` inside `IndicTrans2Adapter` (already) — LiveScreen does not need extra lock but must not run MT parallel with ASR/TTS (hold in same `withContext(Dispatchers.IO)` sequential). Show second DualLangCard live with `santaliText` and keep `DualLangCard(label="Santali — Ol Chiki (MT)", text=santaliText, modifier=weight(1f))`.</action>
  <verify>grep -n "LatencyTracker\|Vachak-Latency\|Vachak-MT" android/app/src/main/java/com/vachak/ui/screens/LiveScreen.kt | head -n 10</verify>
  <acceptance_criteria>
    - "LiveScreen.kt marks LatencyTracker T2 after translate and logs Vachak-Latency/Vachak-MT"
    - "MT latency warn if >500ms present"
  </acceptance_criteria>
  <done>MT latency T2 gated ≤500ms, logged</done>
</task>

</tasks>

<artifacts>
Artifacts this phase produces:
- android/app/src/main/java/com/vachak/ui/screens/LiveScreen.kt (sat_Olck live MT + Ol Chiki + latency T2)
</artifacts>
