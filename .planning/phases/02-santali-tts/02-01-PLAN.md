---
phase: 02-santali-tts
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - ml/tts/train.py
  - ml/tts/dataset/prepare_santali.py
  - models/vits-sat.onnx
  - android/ml/src/main/assets/vachak_models/tts/model.onnx
autonomous: true
requirements: ["TTS-01"]
user_setup: ["VOICE_CONSENT.md signed by speaker(s) before training"]

must_haves:
  truths:
    - "Santali VITS trained on IndicVoices 19,779 + Nirantar 13,503 + Rasa subset (CC BY 4.0 verified)"
    - "Exported model.onnx + tokens.txt + lexicon.txt loadable by sherpa-onnx OfflineTts"
    - "Model size 20–80MB, 22.05kHz mono"
  artifacts:
    - "models/vits-sat.onnx"
    - "ml/tts/dataset/santali_manifest.json"
  key_links:
    - "Dataset manifest → VITS train → ONNX export → sherpa-onnx load"
---

<objective>
Fine-tune Santali VITS (Coqui/MunTTS pattern) and export to ONNX for sherpa-onnx.

Purpose: Replace Chinese vits-zh-aishell3 DEV-FIXTURE with audible Santali voice.
Output: Trained checkpoint + export model.onnx/tokens/lexicon
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-mundari-tts/02-CONTEXT.md
@.planning/phases/02-mundari-tts/02-RESEARCH.md
@AGENTS.md
@docs/PHASES.md
@ml/tts/README.md
</context>

<threat_model>
STRIDE: Tampering (voice clone without consent), Information Disclosure (speaker PII)
ASVS L1: Voice consent recorded before training, dataset licenses verified CC BY 4.0, no PII in repo
</threat_model>

<tasks>

<task type="auto">
  <name>Census + consent + prepare Santali dataset (22.05k mono)</name>
  <files>ml/tts/dataset/prepare_santali.py, VOICE_CONSENT.md</files>
  <read_first>ml/tts/README.md, docs/MODEL_AND_DATA_PROVENANCE.md</read_first>
  <action>Verify CC BY 4.0 for IndicVoices Santali (19,779), Nirantar (13,503/161h), Rasa subset, Common Voice ~533. Record in THIRD_PARTY_NOTICES.md. Get VOICE_CONSENT.md signed (template from RESEARCH.md). Run prepare_santali.py to resample to 22.05k mono, trim silence, build Ol Chiki char tokens vs espeak-ng-data decision, output manifest with speaker count, duration, Ol Chiki coverage. Keep single-speaker subset if multi-speaker degrades (curation per RESEARCH.md).</action>
  <verify>ls ml/tts/dataset/santali_manifest.json && cat VOICE_CONSENT.md | grep -c "consent"</verify>
  <acceptance_criteria>
    - "santali_manifest.json exists with duration and speaker count"
    - "VOICE_CONSENT.md exists"
  </acceptance_criteria>
  <done>Dataset censused, consent signed, manifest built</done>
</task>

<task type="auto">
  <name>Train VITS (300–800 epochs) and export ONNX</name>
  <files>ml/tts/train.py, models/vits-sat.onnx</files>
  <read_first>ml/tts/train.py, sherpa-onnx/README.md</read_first>
  <action>Train VITS via Coqui TTS or MunTTS train_vits.sh pattern: batch 16 on RTX 3050 or 64 on A100, HiFi-GAN vocoder, 4-loss VITS. Use Ol Chiki char tokens (U+1C50–U+1C7F) or espeak-ng-data if needed — decide via ablation. Export single model.onnx opset17 + onnx.checker + tokens.txt (Ol Chiki vocab) + lexicon.txt. Must be <80MB (20–80 budget); if >80 quantize or prune. Copy to android/ml/src/main/assets/vachak_models/tts/ for wiring.</action>
  <verify>ls -lh models/vits-sat.onnx && python -c "import onnx; onnx.checker.check_model('models/vits-sat.onnx'); print('tts onnx ok')"</verify>
  <acceptance_criteria>
    - "models/vits-sat.onnx exists <80MB and checker passes"
    - "tokens.txt contains Ol Chiki range"
  </acceptance_criteria>
  <done>VITS trained and exported ONNX green</done>
</task>

</tasks>

<artifacts>
Artifacts this phase produces:
- ml/tts/dataset/santali_manifest.json
- models/vits-sat.onnx
- android/ml/src/main/assets/vachak_models/tts/model.onnx (+ tokens.txt, lexicon.txt)
</artifacts>
