---
phase: 06-benchmark
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - android/ml/src/main/java/com/vachak/ml/LatencyTracker.kt
  - android/app/src/main/java/com/vachak/ui/DiagnosticsScreen.kt
  - benchmarks/run_benchmark.py
  - docs/benchmarks/BENCHMARK_REPORT.md
autonomous: true
requirements: ["PERF-01", "PERF-02"]
user_setup: ["2GB Android 9+ device (arm64-v8a) for measurement"]

must_haves:
  truths:
    - "Diagnostics shows sequential total <3s (ASR≤1s MT≤0.5s TTS≤1s) with LatencyTracker"
    - "Peak RSS ≤2GB, APK+pack ≤500MB per budget table"
    - "benchmarks/run_benchmark.py reproduces on device + reports p50/p95"
  artifacts:
    - "benchmarks/run_benchmark.py"
    - "docs/benchmarks/BENCHMARK_REPORT.md (green)"
  key_links:
    - "LatencyTracker marks ↔ Diagnostics UI ↔ benchmark report"
---

<objective>
Instrument sequential pipeline, enforce RAM budget, and produce reproducible benchmark report.

Purpose: Prove <3s on 2GB within ~500MB before demo.
Output: Tracker enforcement, harness, report
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/06-benchmark/06-CONTEXT.md
@AGENTS.md
@benchmarks/
</context>

<threat_model>
STRIDE: Denial (OOM), Tampering (benchmark skew)
ASVS L1: Validate sequential (no parallel), cap memory, reproducible runs
</threat_model>

<tasks>

<task type="auto">
  <name>Enforce sequential execution + free handles between legs</name>
  <files>android/app/src/main/java/com/vachak/engine/EngineProvider.kt, android/ml/src/main/java/com/vachak/ml/LatencyTracker.kt</files>
  <read_first>android/app/src/main/java/com/vachak/engine/EngineProvider.kt, android/ml/src/main/java/com/vachak/ml/adapter/IndicTrans2Adapter.kt</read_first>
  <action>Ensure EngineProvider/Orchestrator runs ASR→MT→TTS sequentially (never parallel): after ASR completes, close/release its ORT/Sherpa session before loading MT; after MT close before TTS. Use Kotlin coroutine with single dispatcher or mutex. numThreads=1 for all ORT/Sherpa sessions. Add LatencyTracker marks: asrStart/End, mtStart/End, ttsStart/End, total. Log Vachak-Latency. Surface in DiagnosticsScreen as table with green/red gate (<3s).</action>
  <verify>grep -n "LatencyTracker" android/ml/src/main/java/com/vachak/ml/*.kt android/app/src/main/java/com/vachak/**/*.kt | head && grep -n "sequential\|mutex\|withContext" android/app/src/main/java/com/vachak/engine/EngineProvider.kt | head</verify>
  <acceptance_criteria>
    - "EngineProvider enforces sequential (no parallel launch)"
    - "LatencyTracker marks all 3 legs"
  </acceptance_criteria>
  <done>Sequential enforcement + tracker present</done>
</task>

<task type="auto">
  <name>Benchmark harness (repeatable) + Diagnostics UI</name>
  <files>benchmarks/run_benchmark.py, android/app/src/main/java/com/vachak/ui/DiagnosticsScreen.kt</files>
  <read_first>benchmarks/translation_benchmark.py, docs/benchmarks/BENCHMARK_REPORT.md</read_first>
  <action>Extend benchmarks/run_benchmark.py to run E2E pipeline 10× on device via adb (or local mock if no device): measure asrMs, mtMs, ttsMs, total, peak RSS (adb shell dumpsys meminfo com.vachak), APK+pack size (du -sh). Output JSON + markdown row. DiagnosticsScreen shows live LatencyTracker last run + budget table (App 40–70, ASR 30–80, MT 100–180, TTS 20–80, etc.) with green/red.</action>
  <verify>python benchmarks/run_benchmark.py --help 2>&1 | head && cat docs/benchmarks/BENCHMARK_REPORT.md | tail -30</verify>
  <acceptance_criteria>
    - "benchmarks/run_benchmark.py exists and runs"
    - "DiagnosticsScreen.kt shows LatencyTracker"
  </acceptance_criteria>
  <done>Harness + diagnostics ready</done>
</task>

<task type="auto">
  <name>Measure on 2GB device + write report, handle over-budget</name>
  <files>docs/benchmarks/BENCHMARK_REPORT.md</files>
  <read_first>docs/benchmarks/BENCHMARK_REPORT.md</read_first>
  <action>On target 2GB arm64 device (or emulator with 2GB RAM limit), run harness 3 times, capture p50/p95 total, peak RSS, APK+pack size. Write to BENCHMARK_REPORT.md table. If total >3s: profile per-leg, propose quantize/prune/offload to pack. If storage >500: flag variance like P1-size-variance.md. Report must state sequential and offline.</action>
  <verify>cat docs/benchmarks/BENCHMARK_REPORT.md | grep -E "total|p50|RSS|500"</verify>
  <acceptance_criteria>
    - "BENCHMARK_REPORT.md contains measured total <3s or variance note"
    - "RSS ≤2GB and storage ≤500MB or variance"
  </acceptance_criteria>
  <done>Report green or variance documented</done>
</task>

</tasks>

<artifacts>
Artifacts this phase produces:
- benchmarks/run_benchmark.py
- docs/benchmarks/BENCHMARK_REPORT.md
- android/app/src/main/java/com/vachak/ui/DiagnosticsScreen.kt
</artifacts>
