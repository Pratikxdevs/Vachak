---
phase: 01-ondevice-mt
plan: "02"
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - android/ml/build.gradle.kts
  - android/ml/src/main/java/com/vachak/ml/adapter/IndicTrans2Adapter.kt
  - android/ml/src/main/java/com/vachak/ml/IndicProcessorPort.kt
  - android/app/src/main/java/com/vachak/engine/EngineProvider.kt
  - THIRD_PARTY_NOTICES.md
  - docs/MODEL_AND_DATA_PROVENANCE.md
autonomous: true
requirements: ["MT-01", "MT-03"]
user_setup: []

must_haves:
  truths:
    - "TranslationEngine translates Hindi string to Ol Chiki without network"
    - "EngineProvider.real(context) returns IndicTrans2Adapter (no MockTranslationEngine)"
    - "LatencyTracker records MT leg ≤0.5s target (log Vachak-MT)"
  artifacts:
    - "android/ml/src/main/java/com/vachak/ml/adapter/IndicTrans2Adapter.kt implements TranslationEngine"
    - "android/app/src/main/java/com/vachak/engine/EngineProvider.kt wires real adapter"
  key_links:
    - "IndicTrans2Adapter ↔ ORT Mobile sessions (encoder/decoder/past) ↔ SherpaAssets mt dir"
    - "EngineProvider.real ↔ IndicTrans2Adapter ↔ LessonTranslatorScreen"
---

<objective>
Build :ml IndicTrans2Adapter (SP tokenize → ORT Mobile greedy decode → Ol Chiki decode) and wire EngineProvider.real() to it.

Purpose: Replace MockTranslationEngine_mock ([DEV-FIXTURE]) with real on-device Hin→Santali. This is the APK path that Python CT2 proved.
Output: Kotlin adapter, ORT Mobile dep, ported IndicProcessor, wired provider, provenance
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-ondevice-mt/01-CONTEXT.md
@.planning/phases/01-ondevice-mt/01-RESEARCH.md
@android/ml/src/main/java/com/vachak/ml/SherpaAssets.kt
@android/app/src/main/java/com/vachak/engine/EngineContracts.kt
@android/app/src/main/java/com/vachak/engine/EngineProvider.kt
@android/ml/src/main/java/com/vachak/ml/SherpaOnnxTtsAdapter.kt
</context>

<threat_model>
STRIDE: Spoofing (fake sat_Olck tag), Tampering (model file swap), Elevation (reflection via tokenizer JSON)
ASVS L1: Validate language codes, no dynamic code loading, clamp token IDs, validate Ol Chiki range U+1C50–U+1C7F
Mitigations: supports() whitelist, id clamp < dict_size else unk_id, OlChikiValidator regex, no reflection
</threat_model>

<tasks>

<task type="auto">
  <name>Add onnxruntime-android to :ml and verify no .so duplication</name>
  <files>android/ml/build.gradle.kts</files>
  <read_first>android/ml/build.gradle.kts, android/app/build.gradle.kts, android/ml/libs/sherpa-onnx-1.13.0.aar</read_first>
  <action>In android/ml/build.gradle.kts add:
implementation("com.microsoft.onnxruntime:onnxruntime-android:1.18.0")
Keep sherpa-onnx AAR (it bundles libonnxruntime.so). After add, run ./gradlew :ml:assembleDebug :app:assembleDebug --no-daemon and check for DuplicateFileException on libonnxruntime.so. If collision, alternative is to reuse sherpa's ORT via its native bridge — document fallback. Must compile with minSdk 28. SherpaAssets pattern stays: prepare(context,"mt") copies mt/ to filesDir; adapter passes null AssetManager is NOT needed — ORT uses file paths.</action>
  <verify>./gradlew :ml:assembleDebug --no-daemon 2>&1 | tail -20 && unzip -l android/ml/build/outputs/aar/ml-debug.aar 2>/dev/null | grep onnxruntime || echo "aar check done"</verify>
  <acceptance_criteria>
    - "android/ml/build.gradle.kts contains onnxruntime-android:1.18.0"
    - "assembleDebug succeeds with no DuplicateFileException"
  </acceptance_criteria>
  <done>ORT Mobile dependency added, build green</done>
</task>

<task type="auto">
  <name>Port minimal IndicProcessor pre/postprocess to Kotlin</name>
  <files>android/ml/src/main/java/com/vachak/ml/IndicProcessorPort.kt</files>
  <read_first>ml/translation/mundari/it2_ct2_baseline.py, indictrans2-onnx-export/src/it2_inference.py</read_first>
  <action>Port only preprocess_batch/postprocess_batch surface needed for Hin→Sat:
- NFKC normalize, lang-tag prefixing (hin_Deva → sat_Olck), placeholder handling for numbers/URLs, clearing _placeholder_entity_maps queue between calls
- Must match Python IndicProcessor(inference=True) on 20 fixtures (write test in androidTest or ml unit test that asserts Kotlin pre/post == Python output for 20 samples)
Do NOT import IndicTransToolkit at runtime — reimplement minimal rules + bundle indic_nlp_resources if needed under assets. Keep logic <200 lines.</action>
  <verify>./gradlew :ml:testDebugUnitTest --tests "*IndicProcessorPortTest*" --no-daemon 2>&1 | tail -20</verify>
  <acceptance_criteria>
    - "IndicProcessorPort.kt exists and contains fun preprocessBatch"
    - "Unit test passes: Kotlin pre/post matches Python on 20 fixtures"
  </acceptance_criteria>
  <done>Processor port implemented and tested</done>
</task>

<task type="auto">
  <name>Implement IndicTrans2Adapter (greedy decode ORT loop) + Ol Chiki validator</name>
  <files>android/ml/src/main/java/com/vachak/ml/adapter/IndicTrans2Adapter.kt</files>
  <read_first>indictrans2-onnx-export/src/it2_inference.py, android/ml/src/main/java/com/vachak/ml/SherpaAssets.kt, android/app/src/main/java/com/vachak/engine/EngineContracts.kt</read_first>
  <action>Class IndicTrans2Adapter(context:Context): TranslationEngine {
  baseDir = SherpaAssets.prepare(context,"mt");
  env = OrtEnvironment.getEnvironment(); opts = SessionOptions().apply { setInterOpNumThreads(1); setIntraOpNumThreads(1) };
  enc = env.createSession("$baseDir/encoder_model.onnx",opts); dec, decPast similarly lazy;
  Load tokenizer_src/tgt.json via Rust tokenizers (or JSON parse + vocab map) + meta (src_dict_size/tgt_dict_size/unk_id:3/decoder_start_id:2/eos_id:2);
  translate(text,pair): if !supports(pair) return Err(UNSUPPORTED_LANGUAGE); if blank Err(INVALID_INPUT);
  Preprocess → encode (clamp ids < src_dict_size else unk_id, pad, attention_mask) → enc.run → dec.run(step0: decoder_start_id + encOut + mask) → loop decPast with past_key_values.* (shape batch,8,1,64 + enc KVs), argmax logits, append, stop on eos 2 or max 128 → clamp ids < tgt_dict_size → batch_decode → postprocess → validate Ol Chiki regex [\u1C50-\u1C7F] → log Vachak-MT + LatencyTracker mark → Ok(sat)
  supports(): source in hi/hin/hindi, target in sat/sat_Olck/olck (also mund/mun alias to sat for compat)
  Must run on Dispatchers.IO/Default, sequential only, Log.d TAG Vachak-MT at input/tokenize/decode/output/latency.
}</action>
  <verify>./gradlew :ml:assembleDebug --no-daemon && grep -n "class IndicTrans2Adapter" android/ml/src/main/java/com/vachak/ml/adapter/IndicTrans2Adapter.kt && grep -n "Vachak-MT" android/ml/src/main/java/com/vachak/ml/adapter/IndicTrans2Adapter.kt</verify>
  <acceptance_criteria>
    - "IndicTrans2Adapter.kt contains class IndicTrans2Adapter : TranslationEngine"
    - "IndicTrans2Adapter.kt contains Vachak-MT log"
    - "supports() handles sat_Olck and olck"
  </acceptance_criteria>
  <done>Adapter implements contract, logs, validates Ol Chiki</done>
</task>

<task type="auto">
  <name>Wire EngineProvider.real() to IndicTrans2Adapter and update provenance</name>
  <files>android/app/src/main/java/com/vachak/engine/EngineProvider.kt, THIRD_PARTY_NOTICES.md, docs/MODEL_AND_DATA_PROVENANCE.md</files>
  <read_first>android/app/src/main/java/com/vachak/engine/EngineProvider.kt, THIRD_PARTY_NOTICES.md</read_first>
  <action>Change EngineProvider.real(context) translation = IndicTrans2Adapter(context) (was MockTranslationEngine). Keep mock() unchanged for tests. In THIRD_PARTY_NOTICES.md append IndicTrans2 MIT + COILD CC BY 4.0 + ONNX Runtime MIT with repo + model names + licenses. In MODEL_AND_DATA_PROVENANCE.md add row: IndicTrans2 dist-320M | AI4Bharat | MIT | Hin→Sat MT (ONNX int8) | WIRED (P1). Do NOT add INTERNET permission.</action>
  <verify>grep -n "IndicTrans2Adapter" android/app/src/main/java/com/vachak/engine/EngineProvider.kt && grep -n "indictrans2" THIRD_PARTY_NOTICES.md -i && cat android/app/src/main/AndroidManifest.xml | grep -c INTERNET || echo "no internet ok"</verify>
  <acceptance_criteria>
    - "EngineProvider.kt real() contains IndicTrans2Adapter"
    - "THIRD_PARTY_NOTICES.md contains IndicTrans2"
    - "AndroidManifest.xml does not contain android.permission.INTERNET"
  </acceptance_criteria>
  <done>Wiring swapped, provenance recorded, no internet permission</done>
</task>

</tasks>

<artifacts>
Artifacts this phase produces:
- android/ml/src/main/java/com/vachak/ml/adapter/IndicTrans2Adapter.kt
- android/ml/src/main/java/com/vachak/ml/IndicProcessorPort.kt
- android/ml/build.gradle.kts (with onnxruntime-android)
- THIRD_PARTY_NOTICES.md (updated)
</artifacts>
