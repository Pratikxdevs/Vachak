---
phase: 01-ondevice-mt
plan: "03"
type: execute
wave: 3
depends_on: ["01-02"]
files_modified:
  - android/ml/src/test/java/com/vachak/ml/IndicTrans2AdapterTest.kt
  - android/app/src/androidTest/java/com/vachak/TranslationEngineTest.kt
  - benchmarks/translation_benchmark.py
  - docs/benchmarks/BENCHMARK_REPORT.md
autonomous: true
requirements: ["MT-01", "MT-03"]
user_setup: []

must_haves:
  truths:
    - "On-device unit test: Hindi fixture → non-empty Ol Chiki string, no network"
    - "Device test WiFi OFF: Translate lesson shows Ol Chiki, ≤0.5s"
    - "APK has zero INTERNET calls (audit)"
  artifacts:
    - "android/ml/src/test/java/com/vachak/ml/IndicTrans2AdapterTest.kt"
    - "benchmarks/translation_benchmark.py"
  key_links:
    - "Adapter test ↔ real ONNX bundle (not mock)"
    - "LatencyTracker ↔ Vachak-MT logs ↔ benchmark report"
---

<objective>
Verify P1 offline: unit tests, device WiFi-OFF test, latency gate ≤0.5s, no-network audit.

Purpose: Prove MT-01/MT-03 without hidden internet and that UI shows real Santali.
Output: Green tests, benchmark report, logcat evidence
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-ondevice-mt/01-CONTEXT.md
@.planning/phases/01-ondevice-mt/01-RESEARCH.md
@android/ml/src/main/java/com/vachak/ml/adapter/IndicTrans2Adapter.kt
@android/ml/src/main/java/com/vachak/ml/LatencyTracker.kt
</context>

<threat_model>
STRIDE: Information Disclosure (test leaks IN22), Denial (slow MT >0.5s blocks demo)
ASVS L1: No network in test, do not include IN22-Gen/Conv in fixtures, bound decode length 128
Mitigations: audit harness checks INTERNET absent + no HttpURLConnection/OkHttp in :ml, fixture is FlN sentence not IN22
</threat_model>

<tasks>

<task type="auto">
  <name>Unit test: fixture Hindi → Ol Chiki, no mock, no network</name>
  <files>android/ml/src/test/java/com/vachak/ml/IndicTrans2AdapterTest.kt</files>
  <read_first>android/ml/src/main/java/com/vachak/ml/adapter/IndicTrans2Adapter.kt, android/app/src/main/java/com/vachak/engine/EngineContracts.kt</read_first>
  <action>Create IndicTrans2AdapterTest with:
- setUp loads adapter via ApplicationProvider.getApplicationContext()
- testTranslateFixture(): "बच्चों, पाँच आम गिनो।" with pair hi->sat → result is Ok and string contains [\u1C50-\u1C7F] and not contains "[DEV-FIXTURE"
- testUnsupportedLanguage(): pair hi->eng returns Err UNSUPPORTED_LANGUAGE
- testNoNetwork(): grep :ml sources for "http", "OkHttp", "INTERNET" → must be absent (or use network_request_audit)
Must use real ONNX bundle, not mock. Run on JVM with Robolectric or on-device via androidTest if ORT needs native.</action>
  <verify>./gradlew :ml:testDebugUnitTest --tests "*IndicTrans2AdapterTest*" --no-daemon 2>&1 | tail -30</verify>
  <acceptance_criteria>
    - "IndicTrans2AdapterTest.kt contains DEV-FIXTURE negative check"
    - "test passes with real ONNX bundle"
  </acceptance_criteria>
  <done>Unit test green, real translation verified</done>
</task>

<task type="auto">
  <name>Device WiFi-OFF verification: Translate lesson → Ol Chiki, log Vachak-MT</name>
  <files>android/app/src/main/java/com/vachak/ui/MainScreen.kt</files>
  <read_first>android/app/src/main/java/com/vachak/ui/MainScreen.kt, android/ml/src/main/java/com/vachak/ml/LatencyTracker.kt</read_first>
  <action>Manual + automated: install APK, set device WiFi OFF + airplane on, open Vachak → lesson loads → tap Translate lesson → assert translatedText matches Ol Chiki regex and LatencyTracker mtMs <=500. Capture: adb logcat -s Vachak-MT Vachak-Assets Vachak-Latency. Log must show input/tokenize/decode/output/latency tags from adapter. Take screenshot of translated lesson. Do NOT attempt to bypass offline — test must fail if mock fallback used.</action>
  <verify>adb logcat -s Vachak-MT -d 2>&1 | grep -c "Vachak-MT" && adb shell dumpsys package com.vachak | grep permission</verify>
  <acceptance_criteria>
    - "logcat contains Vachak-MT entries for translate"
    - "UI shows Ol Chiki string (codepoints U+1C50–U+1C7F)"
    - "mtMs <= 500"
  </acceptance_criteria>
  <done>Device test passes WiFi OFF, logs captured</done>
</task>

<task type="auto">
  <name>No-network audit + benchmark report update</name>
  <files>benchmarks/translation_benchmark.py, docs/benchmarks/BENCHMARK_REPORT.md, ml/benchmarks/harness.py</files>
  <read_first>ml/benchmarks/harness.py, benchmarks/translation_benchmark.py</read_first>
  <action>Run ml/benchmarks/harness.network_request_audit() or grep -r "INTERNET\|http\|OkHttp\|HttpURLConnection" android/ml android/app --include="*.kt" --include="*.xml" → must be zero hits. Run benchmarks/translation_benchmark.py to measure mt latency on device (or emulator x86_64) for 10 Fixtures, record p50/p95. Append row to docs/benchmarks/BENCHMARK_REPORT.md: P1 MT | int8 | size MB | p50 ms | p95 ms | pass <=500. Also du -sh android/ml/src/main/assets/vachak_models/mt for report.</action>
  <verify>grep -r "android.permission.INTERNET" android/ --include="*.xml" | wc -l && cat docs/benchmarks/BENCHMARK_REPORT.md | tail -20</verify>
  <acceptance_criteria>
    - "grep INTERNET returns 0"
    - "BENCHMARK_REPORT.md contains P1 MT row with p50 and size"
  </acceptance_criteria>
  <done>Audit clean, report updated</done>
</task>

</tasks>

<artifacts>
Artifacts this phase produces:
- android/ml/src/test/java/com/vachak/ml/IndicTrans2AdapterTest.kt
- benchmarks/translation_benchmark.py (updated)
- docs/benchmarks/BENCHMARK_REPORT.md (P1 row)
- logcat Vachak-MT evidence (not committed, attached to PR)
</artifacts>
