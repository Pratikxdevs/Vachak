---
phase: 04-curriculum
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - curriculum/lessons/sat_lessons.json
  - android/content/src/main/java/com/vachak/content/ContentEngine.kt
  - android/content/src/main/java/com/vachak/content/db/LessonDao.kt
  - android/content/src/main/java/com/vachak/content/db/AppDatabase.kt
  - android/app/src/main/java/com/vachak/ui/LessonScreen.kt
autonomous: true
requirements: ["CUR-01", "CUR-02", "CUR-03"]
user_setup: []

must_haves:
  truths:
    - "Lesson list/detail renders from Room with real FLN lessons (not DEV-FIXTURE)"
    - "Each lesson shows NIPUN outcome mapping"
    - "Worksheet and flashcard buttons open template/prebuilt content offline"
  artifacts:
    - "curriculum/lessons/sat_lessons.json"
    - "android/content/src/main/java/com/vachak/content/ContentEngine.kt"
  key_links:
    - "curriculum/*.json → Room prepopulate → ContentEngine → LessonScreen"
---

<objective>
Author Santali FLN curriculum and ship via Room (lessons + NIPUN + worksheets + flashcards).

Purpose: Replace MockCurriculumEngine with real pedagogy.
Output: Authored JSON + Room DB + UI binding
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/04-curriculum/04-CONTEXT.md
@AGENTS.md
@curriculum/
@android/content/
</context>

<threat_model>
STRIDE: Tampering (curriculum substitution), Information Disclosure (none)
ASVS L1: Validate lesson JSON schema, no code exec from curriculum files, hash in P5 manifest
</threat_model>

<tasks>

<task type="auto">
  <name>Author Santali FLN lessons + NIPUN mapping (precomputed translations)</name>
  <files>curriculum/lessons/sat_lessons.json, curriculum/outcomes/nipun.json</files>
  <read_first>AGENTS.md, curriculum/</read_first>
  <action>Author 5–10 FLN lessons in curriculum/lessons/sat_lessons.json: each {id, grade(1-3), domain(oral/reading/writing), titleHi, textHi, textSatOlChiki (precomputed via P1 model or human-reviewed), outcomeId, mediaRefs}. Map outcomes in curriculum/outcomes/nipun.json: {outcomeId, nipunCode, descriptor, grade}. DO NOT use on-device MT at runtime for this store — translations are precomputed and reviewed per AGENTS.md. Validate Ol Chiki codepoints in textSatOlChiki.</action>
  <verify>cat curriculum/lessons/sat_lessons.json | grep -c "textSatOlChiki" && cat curriculum/outcomes/nipun.json | grep -c "nipunCode"</verify>
  <acceptance_criteria>
    - "sat_lessons.json has >=5 lessons with textHi + textSatOlChiki"
    - "nipun.json has mapping for each lesson outcomeId"
  </acceptance_criteria>
  <done>Lessons and outcomes authored, Ol Chiki validated</done>
</task>

<task type="auto">
  <name>Build Room schema + ContentEngine + prepopulate</name>
  <files>android/content/src/main/java/com/vachak/content/db/AppDatabase.kt, android/content/src/main/java/com/vachak/content/ContentEngine.kt</files>
  <read_first>android/content/src/main/java/com/vachak/content/db/AppDatabase.kt</read_first>
  <action>Entities: Lesson(id, titleHi, textHi, textSat, outcomeId), Outcome(id, code, descriptor), Worksheet(id, lessonId, templateType, assetPath), Flashcard(id, lessonId, imagePath, hi, sat). DAOs + AppDatabase with createFromAsset prepopulate from curriculum/lessons JSON (or Room callback). ContentEngine exposes getLesson(id), getLessons(), getOutcome(lesson), getWorksheets(lesson), getFlashcards(lesson). Wire EngineProvider to real ContentEngine (drop MockCurriculumEngine).</action>
  <verify>grep -n "class ContentEngine" android/content/src/main/java/com/vachak/content/ContentEngine.kt && grep -n "@Entity" android/content/src/main/java/com/vachak/content/db/*.kt</verify>
  <acceptance_criteria>
    - "ContentEngine.kt implements getLesson"
    - "Room entities exist for Lesson/Outcome/Worksheet/Flashcard"
  </acceptance_criteria>
  <done>Room DB and ContentEngine ready</done>
</task>

<task type="auto">
  <name>Template worksheets + prebuilt flashcards + UI binding</name>
  <files>worksheet/templates/*.pdf, flashcard/assets/*.png, android/app/src/main/java/com/vachak/ui/LessonScreen.kt</files>
  <read_first>android/app/src/main/java/com/vachak/ui/LessonScreen.kt</read_first>
  <action>Add 2–3 worksheet templates per lesson type (fill-in, match, trace) as PDFs under worksheet/templates/ (or Compose→PdfDocument generation, but templates not AI-gen). Build flashcard PNGs under flashcard/assets/ with hi+satOlChiki+image. Bind LessonScreen to ContentEngine: shows lesson textHi + textSat, outcome badge, buttons Worksheets/Flashcards that open PDF/image viewer offline (no network, no generation).</action>
  <verify>ls worksheet/templates/ | wc -l && ls flashcard/assets/ | wc -l && grep -n "ContentEngine" android/app/src/main/java/com/vachak/ui/LessonScreen.kt</verify>
  <acceptance_criteria>
    - "worksheet/templates has >=3 PDFs"
    - "flashcard/assets has >=5 PNGs"
    - "LessonScreen.kt reads from ContentEngine not mock"
  </acceptance_criteria>
  <done>Worksheets/flashcards built and UI bound, offline</done>
</task>

</tasks>

<artifacts>
Artifacts this phase produces:
- curriculum/lessons/sat_lessons.json
- curriculum/outcomes/nipun.json
- android/content/src/main/java/com/vachak/content/** (Room)
- worksheet/templates/*.pdf
- flashcard/assets/*.png
</artifacts>
