---
phase: 15-content-viewer
plan: "01"
type: execute
wave: 1
depends_on: ["14-01"]
files_modified:
  - "scripts/build_content_pack.py"
  - "android/app/src/main/assets/pdf_pool/"
  - "android/app/src/main/java/com/vachak/ui/content/PackContentReader.kt"
  - "android/app/src/main/java/com/vachak/ui/screens/PackStudyViews.kt"
autonomous: true
requirements: ["CV-01", "CV-02", "CV-03"]
user_setup:
  - "Fresh-install the APK on the tablet"
---

<objective>
Content viewer hardening per user directive (2026-09-16): pool PDFs open via
THREE independent paths (installed pack → bundled pack → direct APK assets);
worksheet images full-visibility; text-only worksheets open as before.
</objective>

<tasks>

- [x] **T1 — Direct-asset PDF pool (CV-01)**
  `assets/pdf_pool/` holds the 5 pool PDFs + `mapping.json` (grade/slug →
  file+sha256), refreshed by `build_content_pack.py::export_pdf_assets()` on
  every pack build. `openWorksheetPdfBlocking()` step 3 serves them
  (asset → capped cache copy → sha256 → open). PDFs now open even with zero
  usable packs on device.

- [x] **T2 — Full-visibility images (CV-02)**
  Worksheet preview cap 160dp → 300dp, ContentScale.Fit + full width: entire
  image visible border-to-border.

- [x] **T3 — Text-only worksheets (CV-03)**
  No change needed: fair-share/family-and-friends carry 10 text questions and
  render through the existing JSON path (null-art guarded). Verified via the
  30/30 audit (explicit slots resolve with question counts).

</tasks>

<verification>
- `:app:compileDebugKotlin` + `:app:testDebugUnitTest` green; comment-depth 0/2.
- Clean APK **663.8 MB** (< 750 cap): single v0.3.1 + 5 direct PDFs + mapping.
- No manifest/ml/deck-JSON diffs; no new permissions; no network.
</verification>

<success_criteria>
- [x] Pool PDF opens on a separate in-app viewer screen, readable
- [x] Worksheet images fully visible; text-only sheets open
- [x] APK < 750 MB with all content paths live
</success_criteria>
