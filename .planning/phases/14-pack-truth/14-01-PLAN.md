---
phase: 14-pack-truth
plan: "01"
type: execute
wave: 1
depends_on: ["13-01"]
files_modified:
  - "android/app/src/main/java/com/vachak/ui/content/PackContentReader.kt"
  - "android/app/src/main/java/com/vachak/ui/screens/PackStudyViews.kt"
  - "android/app/src/main/assets/packs/sat_Olck-v0.3.0.vachakpack"
autonomous: true
requirements: ["PT-01", "PT-02", "PT-03", "PT-04"]
user_setup:
  - "Fresh-install the APK on the tablet (or clear app data) so no stale v0.2.0 installed pack lingers in filesDir"
---

<objective>
Pack-truth fixes per user directive (2026-09-16): worksheets show NO answers;
flashcard faces always fit (scroll); the READER prefers the richest source per
chapter so a stale installed v0.2.0 can never shadow v0.3.0 with identical
placeholder decks; single-pack APK (v0.2.0 asset deleted) at 646 MB < 750 MB.
</objective>

<context>
@.planning/phases/12-pdf-worksheets/12-CONTEXT.md
@android/app/src/main/java/com/vachak/ui/content/PackContentReader.kt
@android/app/src/main/java/com/vachak/ui/screens/PackStudyViews.kt
</context>

<threat_model>
STRIDE: Denial (richest-source adds one bounded bundled scan per chapter open when installed is thin; same caps), Tampering (sha256 gates unchanged on both paths), Information Disclosure (none).
ASVS L1: No network, no new permissions, no new routes, no manifest change.
</threat_model>

<tasks>

- [x] **T1 — Root-cause + proof (PT-01)**
  Proved v0.3.0 decks are distinct per chapter (4 distinct sha256, distinct
  first cards) — "same flashcards" came from the DEVICE reading a stale
  installed v0.2.0 (identical placeholder decks), never from the new pack.
  `/cz/Vachak/2/` does not exist (no new authored grade to wire; pool covers
  G2 — drop a `/2/` dir in the 4-file layout and re-run the converter).

- [x] **T2 — Richest-source wins (PT-01, PT-02)**
  `loadChapterBundle`: loads installed AND bundled, picks higher score
  (questions + cards + 1000 for pool PDF); tie → installed. `readGradeCounts`
  merges per slug with the same rule. A stale pack can never shadow richer
  content again, on any device state, with zero version parsing.

- [x] **T3 — Worksheet answers off + deck fit (PT-03)**
  Removed "Answer: X" lines (practice sheets show questions + art only; keys
  stay in pack data). Deck face wrapped in internal scroll (long Ol Chiki
  always fits); blank face "Pending speaker review" → "—".

- [x] **T4 — Single-pack APK + verify (PT-04)**
  Deleted `sat_Olck-v0.2.0.vachakpack` from assets. Clean
  `:app:assembleDebug` → **646.4 MB** (raw 778.7, 298 files, single v0.3.0 at
  18.9 stored). `:app:compileDebugKotlin` + `:app:testDebugUnitTest` green.
  (Note: incremental rebuilds can retain deleted-asset bytes — clean build is
  ground truth; 695 MB interim number was a stale incremental artifact.)
</tasks>

<verification>
- v0.3.0 deck hashes distinct (4/4 G3); 30/30 audit still holds (Wave 1).
- Compile + unit tests green (incl. 12-02's 18 tests, NavRouteTest).
- APK: 646.4 MB clean-measured, one pack, < 750 MB user cap.
- `git status android/`: reader + PackStudyViews modified; v0.2.0 asset
  deleted (gitignored path — local only); no manifest/ml/deck-JSON diffs.
</verification>

<success_criteria>
- [x] No device state can show stale identical decks (richest-source proof)
- [x] Worksheets: questions + art, zero answers, zero review chrome
- [x] Deck faces fit on small tablets; flip behavior unchanged (front-only)
- [x] APK 646 MB < 750 MB, single content pack
</success_criteria>

## Artifacts this phase produces

- `pickRicherBundle()` + score-merged `readGradeCounts()`
- Answer-free `PackWorksheetView`; scroll-fit `PackDeckView`
- Single-pack APK (646.4 MB clean-measured)
- Nothing else.
