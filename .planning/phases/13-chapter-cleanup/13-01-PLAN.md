---
phase: 13-chapter-cleanup
plan: "01"
type: execute
wave: 1
depends_on: ["12-02"]
files_modified:
  - "android/app/src/main/java/com/vachak/ui/screens/ChapterScreen.kt"
  - "android/app/src/main/java/com/vachak/ui/screens/PackStudyViews.kt"
  - "android/app/src/main/java/com/vachak/ui/screens/GradeScreen.kt"
  - "android/app/src/main/java/com/vachak/ui/navigation/PackSummary.kt"
  - "android/app/src/main/java/com/vachak/ui/content/PackContentReader.kt"
autonomous: true
requirements: ["CU-01", "CU-02", "CU-03"]
user_setup: []
---

<objective>
Chapter-page cleanup per user directive (2026-09-16): chapter page shows ONLY
Worksheets + Flashcards (no gallery/Read/questions/notices); the worksheet
"needs review" badge + answer-key sermon lines removed; grade rows show LIVE
worksheet/deck counts from pack data (never stale summary alone) in pack
(demo_set) order.
</objective>

<context>
@.planning/phases/12-pdf-worksheets/12-CONTEXT.md
@android/app/src/main/java/com/vachak/ui/screens/ChapterScreen.kt
@android/app/src/main/java/com/vachak/ui/screens/PackStudyViews.kt
@android/app/src/main/java/com/vachak/ui/screens/GradeScreen.kt
@android/app/src/main/java/com/vachak/ui/navigation/PackSummary.kt
@android/app/src/main/java/com/vachak/ui/content/PackContentReader.kt
</context>

<threat_model>
STRIDE: Denial (counts pass adds one bounded zip scan per grade open, same caps as title-lines pass; installed-pack path is direct small reads), Tampering (none — read-only), Information Disclosure (none).
ASVS L1: No network, no new permissions, no new routes. Chapter route + NavRoute.Chapter path strings unchanged (NavRouteTest green).
</threat_model>

<tasks>

- [x] **T1 — Chapter page: 2 options only (CU-01)**
  Rewrote `ChapterScreen.kt` (346 → ~150 lines): header (back, breadcrumb,
  title, subject) + Worksheets card + Flashcards card. Removed: stats strip,
  picture gallery, Read/assignments list, notices, mark-complete button,
  bundle/art/script/prefs state. Route, function signature, and callbacks
  unchanged — zero nav changes. Missing state kept (bad slug/grade).

- [x] **T2 — Worksheet review badge removal (CU-02)**
  `PackStudyViews.kt`: deleted the amber "needs review" badge; "Key: X
  (auto-extracted, unverified)" → "Answer: X"; dropped the "Answer key
  pending speaker review" line (no answer → no line). needs_review DATA
  fields still parsed (compat), just never rendered. Deck blank-face +
  grade-row fallback notes intentionally left (out of the ordered scope).

- [x] **T3 — Live counts + pack order (CU-03)**
  `PackContentReader.readGradeCounts()` (installed direct reads → bundled
  single-scan, capped, cancellable, null-safe) + `ChapterCounts` data class.
  `GradeScreen` loads counts alongside title lines; rows show live numbers
  with "PDF worksheet • N flashcards" for pool slots, summary numbers as
  fallback. `PackSummary.loadPackSummary` no longer re-sorts (demo_set order
  preserved).

</tasks>

<verification>
- `:app:compileDebugKotlin` SUCCESSFUL; `:app:testDebugUnitTest` SUCCESSFUL
  (incl. NavRouteTest — routes unchanged; 12-02's 18 new tests green).
- Comment-depth audit 0/7 files (nested-`/*` lesson from Phase 12 applied).
- `git diff --stat android/` limited to the 5 files above; no deck JSON,
  no manifest, no ml changes.
</verification>

<success_criteria>
- [x] Chapter page: header + 2 cards, nothing else
- [x] No "needs review" badge/sermon on worksheets
- [x] Grade rows show live counts in pack order
- [x] Worksheet opens (12-02 PDF/JSON branches unchanged, now unblocked)
</success_criteria>

## Artifacts this phase produces

- Gutted `ChapterScreen.kt` (header + 2 PracticeCards)
- `PackContentReader.readGradeCounts()` + `ChapterCounts`
- Live-count wiring in `GradeScreen.kt`; order-preserving `PackSummary.kt`
- Nothing else.
