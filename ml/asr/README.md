# ml/asr — ASR benchmarking & adapter (skeleton)

Reference candidates for Hindi ASR on 2GB RAM: IndicConformer / Vosk / whisper.cpp.
Benchmark on-device before choosing. This dir holds benchmarking scripts only;
the on-device adapter lives in `android/ml/.../adapter/SherpaAsrAdapter.kt`.
No model is trained in this skeleton build.
