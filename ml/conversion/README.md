# ml/conversion — model → ONNX conversion

Holds export scripts for ASR/TTS/MT to ONNX Runtime Mobile. MT decoder ONNX is
currently blocked (see process.md); reproduce `indictrans2-onnx-export` to unblock.
